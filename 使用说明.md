# FlowTask 使用说明（给第一次拿到这个文件夹的人）

适用于把 `FlowTask` 整个文件夹拷到自己电脑上的同事。版本以同目录
[README.md](README.md) / [CHANGELOG.md](CHANGELOG.md) 顶部为准。

> 先记住两件事：
> **① 文件夹就是数据库** —— 所有数据都是这个目录里的 `.json` 文件，拷走文件夹 = 带走全部数据。
> **② 打开方式要用快捷方式** —— 双击 `FlowTask.lnk`（或 `启动 FlowTask.vbs`），**不要**直接双击
> `.html` 文件。直接双击网页打不开文件存储，数据只会存在浏览器缓存里，换台电脑就没了。

---

## 1. 新电脑上手（三步）

1. 把整个 `FlowTask` 文件夹拷到本机任意位置（例如 `D:\FlowTask`）。不需要安装任何东西。
2. 双击 **`环境检查与部署.bat`**。它会检查运行环境（PowerShell / Node）、数据文件、5178 端口，
   然后自动启动存储服务、生成带图标的 `FlowTask` 快捷方式、打开浏览器。
3. 以后每天只用双击 **`FlowTask`** 快捷方式即可（它会自动把服务带起来，不会弹黑色命令行窗口）。

用完想主动关掉后台服务：双击 `停止存储服务.bat`。不关也没关系，它只在收到请求时才读写文件。

**关于运行时**：启动器会优先找电脑上已有的 `node.exe`（先看 PATH，再看
`%USERPROFILE%\.workbuddy\binaries\node\versions` 里版本号最大的），找不到就用 Windows 自带的
PowerShell 版服务（`flowtask_server.ps1`）。两个版本功能对等、有专门的对等测试，
所以**同一份数据在两种运行时下都能用**，但同一时间只能有一个在跑（都占 5178 端口）。

---

## 2. 账号：谁能建、怎么建

账号**只由管理员开通**（默认关闭自助注册，登录页不会出现注册入口）。

- **新建成员**：管理员进「成员管理」→ 点「新建成员账户」→ 填用户名/显示名/选角色 →
  系统会随机生成一个初始口令（已排除易混的 `0/O/1/l`），可一键复制，当面或私聊发给本人。
- **角色**：管理员（全部权限 + 管账号）/ 成员（建项目、管任务）/ 访客（只读，不能改任何东西）。
- **对方第一次登录**会被引导改掉初始口令（不强制锁人，可以关掉提示继续用，但成员列表里会一直
  显示「待改密」标记，直到 TA 自己改过）。
- **对方忘记密码**：管理员在成员列表点「重置密码」，设一个新口令，旧口令立刻作废。
- **停用 / 启用**：临时不想让某人登录就用停用，随时可以启用回来，他的数据完全不动。
- **删除账户**：不可逆。删之前确认框会说清楚——他的个人库**改名留底**成
  `deleted_flowtask_data_<他的ID>_<时间>.json`（不会真删文件），别人库里的引用统一显示为
  「已注销用户」，需要另行改派名下任务。**只是想不让他登录，请用「停用」。**

⚠️ **两件请现在就做的事**：自带的演示账号 `admin / admin123` 是公开口令，正式使用前请改掉或停用；
另外 `member / member123`、`guest / guest123` 同理。

---

## 3. 共享文件夹（多电脑同步）怎么改

同步是**本地优先**：你手上的文件才是主数据，共享盘只是镜像，所以断网/盘不可达时你照常能用。

**改路径（推荐在界面里改，不用碰配置文件、不用重启）**：管理员进「数据管理 → 共享盘同步」，
填入新的共享目录（例如 `\\fileserver\share\FlowTask`）→ 保存。
系统会**先探后确认**：能连通、能写、目录结构对不对，检查通过才真的切换；路径打错会当场拒绝，
不会凭空建一个空目录然后谎称"已同步"。「停用同步」只关开关，路径仍然留着，再启用不必重敲。

配置文件是 `flowtask_data` 同目录下的 `flowtask_config.json`（可参考 `flowtask_config.example.json`）：

| 键 | 含义 |
| --- | --- |
| `shareDir` | 共享目录（用户配的"意图"，关同步也留着） |
| `syncEnabled` | 同步开关，`false` = 停用但保留路径 |
| `openRegistration` | `true` 才恢复自助注册（默认只允许管理员开号） |

优先级：**环境变量 / 启动参数 > 配置文件 > 默认关闭**。环境变量（`FLOWTASK_SHARE_DIR`、
`FLOWTASK_PORT`、`FLOWTASK_DATA_DIR`）会覆盖配置文件，界面里会明确提示"当前被环境变量覆盖"，
免得你改了配置却没生效还以为是 bug。

共享盘上只有存储、不跑任何服务，因此目录结构是这样组织的：

```
<共享目录>\team\     账户表 + 团队共享库（所有人共用）
<共享目录>\users\    每人一个子目录，只有本人写得进去（单写者，天然不撞车）
<共享目录>\locks\    跨机器写锁（避免两台电脑同时推同一份）
<共享目录>\conflict\ 无法自动裁决时整包留底在这里
```

改动会自动推上去（保存后几秒内），同事那边**每 30 秒自动拉一次**，所以不用互相喊"你同步一下"。
两台电脑同时改了同一个项目的同一个字段才叫冲突，会做实体级三路合并；只有真的裁定不了才留底
到 `conflict\` 并告诉你自动合并了什么、哪里需要你确认。

---

## 4. 项目的两种归属（**这一节最容易踩坑，请务必读完**）

| | 个人项目 | 团队共享项目 |
| --- | --- | --- |
| 数据存在哪 | **你自己**的个人库文件 `flowtask_data_<你的ID>.json` | 团队库 `flowtask_shared.json` |
| 谁能看到 | 只有你本人（服务端只把这份文件发给你） | 项目成员名单里的人，登录后都能看到并编辑 |
| 侧栏标记 | 🔒 个人 / **⚠️ 个人项目但成员名单里写了同事** | 🌐 团队共享 |

**关键规则（新版行为）**：在个人项目里把同事加进成员名单并保存，项目会**自动共享给团队**——
连下面的任务、通知一起搬进团队库。过去"加了人对方却什么都看不到"的静默失效不会再发生了。

配套的两条约束是有意的，别当成 bug：

- **移出同事 ≠ 取消共享**。项目仍留在团队库里（名单里还有别人就继续对他们可见；只剩你时别人
  界面上看不到，但文件仍在 `team\` 里）。想让数据真正回到你自己的库，要手动收回。
- **收回只能由项目创建人本人做**（管理员也不行）。因为"收回"= 把整项目连任务搬回**创建者**的
  个人库文件；管理员替别人收回，会让原创建人从此哪个库都看不到它。管理员要调整协作范围，
  用成员管理把人移出即可。

**可见性只认成员名单，管理员也不例外。** 把人移出项目后，他重新登录（或等 30 秒自动同步）
就再也看不到这个项目——哪怕他的账户角色是管理员。管理员若确实需要纵览全部项目
（例如找回没人认领的共享项目），点侧栏「项目」标题右边的 **👁「显示全部项目」**：
它只是让界面多显示出来，不改动任何数据，且这个开关**只记在你这台电脑上**。

切换入口在「项目设置」弹窗底部：个人项目显示「🌐 共享给团队」，共享项目显示「🔒 收回为个人」，
都有确认框和 6 秒撤销；归档状态的项目不允许改归属（先取消归档）。

🔐 **隐私边界要说清楚**：进了团队库的项目，只有成员名单里的人能在**界面上**看到，
但文件本身在共享盘上——**对那个文件夹有读取权限的人都能直接打开 JSON 看到内容**。
所以绩效、薪酬、个人评价这类内容请留在个人项目里。

---

## 5. 备份、导出与找回

- **自动备份**：每份数据文件每 10 分钟最多留一份备份在 `backups\`，每个文件保留最近 40 份
  （文件名形如 `flowtask_data_<ID>_20260924_0930.json`）。
- **冲突副本 / 留底文件**：出现在「数据管理 → 冲突副本」列表里，可以直接在界面下载。
- **手动全量备份**：把整个文件夹复制一份走就行（数据都在 `.json` 里）。
- **导出给同事/领导**：项目页、顶栏、个人设置里都有「导出」，三种出口：
  **表格 CSV**（中文表头、Excel 直接双击不乱码、公式注入已防护）、**Markdown 摘要**（按项目分组，
  可贴进文档/群里）、**JSON 备份**（用于迁移或还原）。
- **导入**：「数据管理」里可导入 JSON，带结构校验，失败会给用人话说得通的原因；
  导入前会自动留底，出问题能找回。

---

## 6. 换电脑 / 交接

1. 老电脑上「数据管理 → 导出 JSON 备份」存一份（或直接把整个文件夹拷走，两者等价）。
2. 新电脑上放到任意目录 → 双击 `环境检查与部署.bat` → 用 `FlowTask` 快捷方式打开。
3. 登录自己的账号。如果这台机器要接着用共享盘同步，进「数据管理 → 共享盘同步」确认目录正确、
   开关是开的即可（30 秒内会自动采纳同事的改动）。
4. 交接给同事时：把账户也一并交给他（成员管理里重置一次口令，并让对方首登改掉）。

---

## 7. 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 打开后显示「⚠ 仅浏览器缓存」 | 你直接双击了 `.html`。改用 `FlowTask` 快捷方式打开，然后点那个状态条重新检测并把缓存里的数据迁到文件 |
| 顶栏一直「⏳ 同步中」 | 共享盘不可达或没写权限。先在资源管理器里打开那个共享路径试试；路径变了就去「数据管理 → 共享盘同步」改 |
| 同事看不到我的项目 | 先确认项目是 🌐 团队共享（🔒/⚠️ 都说明数据还在你个人库里）；再确认对方账号在成员名单里、且 TA 的同步开关是开的 |
| 把某人移出项目，他登录却还看得到 | 先看他的角色：**管理员**可以点侧栏 👁「显示全部项目」，若开着就会看到全部；关掉后移出名单即不可见。若他是普通成员还看得到，多半是他那边还没拉到最新共享库（等 30 秒或让他刷新页面） |
| 密码忘了 | 找管理员「重置密码」；自己登录状态下可在「个人设置」改密 |
| 端口 5178 被别的程序占了 | 关掉那个程序；或临时用环境变量 `FLOWTASK_PORT` 换端口（注意：换端口后快捷方式里的地址也要一起变） |
| 界面空白 / 数据像是没了 | 去同目录 `backups\` 找最近的一份备份，或「数据管理 → 冲突副本」；损坏的文件会被自动隔离成 `*_corrupt_时间.json`，不会静默清空 |

---

## 8. 一页速查：哪个文件是什么

```
FlowTask_本地项目管理平台.html   应用本体（所有功能都在这一个文件里）
flowtask_server.js / .ps1        本地存储服务（Node 版 / PowerShell 版，二选一在跑）
flowtask_sync.*                  共享盘同步引擎        flowtask_merge.*  三路合并内核
FlowTask.lnk / 启动 FlowTask.vbs 日常启动入口（无黑色窗口）
环境检查与部署.bat               新电脑第一步        停止存储服务.bat   关掉后台服务
flowtask_config.json             同步目录与开关（界面里改，不必手编）
flowtask_auth.json               账户表（口令哈希只在本机/团队目录，不下发给网页）
flowtask_shared.json             团队共享库          flowtask_data_<ID>.json  某人的个人库
flowtask_*.base.json             本机同步基线（合并用的"共同祖先"，只在本地，不上盘）
backups\                         自动备份             flowtask_sync.json   同步账本
```

本工具**只产生 `.json` / `.md` / `.html` / `.js`** 这类普通文本文件，
不生成任何受 DRM / 文档管控策略限制的格式，在受管控的电脑上同样开箱即用。
