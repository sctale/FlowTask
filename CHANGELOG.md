# 变更记录（CHANGELOG）

格式参考 Keep a Changelog。提交可用 `git show <hash>` 查看细节。

## [2.1.1] — 2026-09-25 · 杀软误报治理 + 代码签名 + 本地一键出包

### 360 报警的根因与修复
- **根因**：sidecar 是 postject 往官方 node.exe 里注入数据生成的——**OpenJS 的原始
  Authenticode 签名被破坏成残缺状态**（构建时就有 "signature seems corrupted" 警告），
  「带坏签名的二进制」+「完全未签名的 NSIS 安装包」正是启发式杀软的经典触发组合。
- **修复**：全链路签名——sidecar、FlowTask.exe、安装包三个产物统一用代码签名证书
  Authenticode 签名（SHA256 + DigiCert 时间戳），残缺签名被有效签名覆盖。
  本机自签证书由 `desktop/scripts/ensure-cert.ps1` 一次性生成（写入当前用户受信任根，
  pfx 存 `%USERPROFILE%\FlowTaskSign\`，不入库）。
- **对外分发说明**（README 已加）：自签证书只让本机与内网信任链干净；开源发布站
  建议换 OV/EV 代码签名证书消除其他机器的 SmartScreen/杀软告警；被误报时可向 360
  提交白名单复检（附产物 SHA256）。

### 本地一键出包（替代 CI，构建后直接上传）
- 新增 `desktop/build_release.ps1`：版本五处一致性校验 → 打 sidecar → 签 sidecar →
  构建主程序 → 签主程序 → NSIS 打包 → 签安装包 → 汇总 `desktop/dist/<version>/`
  （免安装 exe + 安装包 + sidecar + SHA256SUMS.txt）。
- `-SkipSidecar` 可跳过服务端未变时的 88MB 重打；任何一步签名校验非 Valid 即中断。

### 版本资源
- exe 属性面板信息补全（FileDescription=FlowTask · 本地项目管理平台、CompanyName 等，
  来自 Cargo.toml 元数据）——无名 exe 也是启发式误报诱因之一。

### 版本
- 三端 + tauri.conf + Cargo.toml 同步 2.1.1。

## [2.1.0] — 2026-09-25 · 桌面版：双击 FlowTask.exe 就能用（Tauri 壳 + SEA sidecar）

### 为什么
「打开 HTML → 起服务 → 弹浏览器」这条 vbs/bat 链路对使用者太隐晦，也挡了开源计划。
本版本给出官方桌面形态，同时**不引入第二套服务端实现**——桌面版跑的就是仓库里这份
`flowtask_server.js`，这是为开源负责的单源原则。

### 架构（desktop/ 新目录）
- **Tauri v2 壳（Rust，~200 行）**：只管四件事——探测/复用 5178 上已有服务、
  拉起内嵌 sidecar、开 WebView 窗口（http://127.0.0.1:5178）、托盘常驻。
  关窗=隐藏到托盘（服务继续跑，同事仍可访问）；退出=收掉自己起的 sidecar。
- **sidecar = Node SEA 单文件 exe**：`build-sidecar.cjs` 用 esbuild 把
  server+sync+merge 捆成一个 bundle，`node --experimental-sea-config` + postject
  注入 node.exe 副本（88MB，含运行时，目标机器零依赖）。
- **单源桥**：服务端仅加一行 `FLOWTASK_HTML` 环境变量支持（HTML 打进安装包资源，
  数据目录由壳指向主 exe 所在目录——「拷走文件夹即搬家」的便携哲学保留）。
- 复用检测与 vbs 启动器同判据（/api/ping 认 `"app":"FlowTask"`），
  桌面版与浏览器版、局域网部署可共存互不干扰。

### 产物与用法
- `desktop/src-tauri/target/release/FlowTask.exe`（免安装，放哪跑哪，数据就在哪）
- `bundle/nsis/FlowTask_2.1.0_x64-setup.exe`（用户级安装器，开始菜单含快捷方式）
- 构建：`cd desktop && npm install && npm run build`（需 Rust + VS Build Tools，开源 CI 可复现）
- 图标由 `npx tauri icon ../flowtask.png` 从品牌标直接生成。

### 版本
- 三端同步 2.1.0；`启动 FlowTask.vbs` 等浏览器入口保留，两种形态共存。

## [2.0.2] — 2026-09-25 · 品牌标第三稿：「清单 → 完成」

### 为什么再改
v2.0.1 的「一横 + 一勾」太通用——像任何一个待办 App 的图标，没有品牌记忆点，
也没讲出「项目和任务管理」这层含义。

### 新标识
- 构图：**两条半透明任务线（一长一短，清单感）+ 一道实心大勾从左下扫到右上**——
  「任务在流动，事情会被做完」。透明度分层（40% / 65% / 100%）制造纵深，
  视觉焦点自然落在勾上；三条笔画同一起点左对齐，小尺寸下依然稳。
- 深青底不变；仍是纯几何笔画，16px 到 256px 全档可读。
- 四处统一：顶栏、登录页、`make_icon.js`（渲染内核升级为分层透明度合成）、
  重生成 `flowtask.ico` + `flowtask.png`。

### 版本
- 三端同步 2.0.2（HTML APP_VERSION / Node / PowerShell），README 版本行更新。

## [2.0.1] — 2026-09-25 · 品牌标重设计 + 工作区清理

### 品牌标（v2.0.0 的「任务单+对勾」被否：白色方块在 15px 下发闷、镂空勾看不清）
- 新标识「**一行任务 + 一个大勾**」：白色粗圆头笔画（stroke 2.7/24），任务行短、对勾大且
  右上冲出——「事情流着流着就完成」的动势；深青底不变。
- 四处统一：顶栏 logo、登录页 logo、`make_icon.js`（SDF 内核按同几何重写，笔画改 segSDF
  胶囊并集）、重生成 `flowtask.ico`（16–256 七档真 Alpha）与 `flowtask.png`。

### 工作区清理
- git 移除三轮走查的一次性构建/改造脚本 16 个（`spark-output/audit/_*` 与
  `append_opps/finalize/findings-round2/opportunities-round2/state-round2`）——
  留档只保留三份走查报告 `flowtask.md` / `flowtask-round2.md` / `flowtask-round3.md` 与
  `context/*.json`（下游 Brief/Stories 真正消费的东西）。
- 回收站清掉根目录 7 个历史调试日志（e2e-m8*/e2e-180b/e2e-181/e2e-ux-r3）与 `tests/tmp/`
  （测试会自动重建）。运行时数据（账户表/个人库/共享库/base 快照/backups）一律未动。

### 版本
- 三端同步 2.0.1（HTML APP_VERSION / Node / PowerShell），README 版本行更新。

## [2.0.0] — 2026-09-25 · 品牌独立：摘掉「仿 Asana」的标签，FlowTask 用自己的脸

### 背景
第三轮体验走查（spark-output/audit/flowtask-round3.md）结论：功能层早已和 Asana 分家
（四状态含原创「已暂停」、个人/共享双库、实体级三路合并、导出中心、筛选视图），
但品牌层与五个交互签名还在替 Asana 打广告——珊瑚红主色、三点轨道 logo、
「首页/我的任务/收件箱」三件套、Tab+Q、三色心情「状态更新」。本轮全部换成自己的。

### 品牌层
- **主色**：珊瑚红 → 深青 `#0F766E`（--brand-dark `#0B5D56`、--brand-light `#E9F2F0`）；
  色池首色、聚焦环、控制台横幅同步。主按钮继续走 --brand-dark（白字 ≈5:1，AA 达标）。
- **Logo**：三点+连线（Asana 轨道标）→「任务单 + 对勾」：白色圆角单据 + 品牌色镂空对勾。
  顶栏、登录页、flowtask.ico/png（make_icon.js 重写绘制内核）四处统一。
- **定位语**：README/DESIGN.md 的「仿 Asana」与源码 13 处「对标 Asana」注释清零，
  改为陈述自身设计意图；DESIGN.md 升 v2.0.0（色彩、术语、栅格命名同步）。

### 命名（只换壳不换功能，路由/字段/内部 key 全部不动）
- 侧栏：首页 → **今天**；收件箱 → **通知**（我的任务保留——行业通用词）。
- 项目视图页签：概览 → **详情**；进度 → **排期**（列表/看板/日历为品类通用，保留）。
- 状态更新 + 三色心情 → **项目播报**（DB 字段仍叫 statusUpdates，导入导出兼容旧备份）。

### 交互签名
- **Tab+Q（Asana 招牌）退役**，换成自己的：**Ctrl+K 命令面板**——搜索任务/项目、
  新建任务/项目、跳转页面、切换当前项目视图，↑↓/Enter/Esc 全键盘操作。
- 快捷键文案单一来源 `KEYMAP`：帮助面板从它渲染，改键不再人肉同步三处。
- 首次引导第 2 步改教 Ctrl+K。

### 视觉细节
- 侧栏/今天页/项目标题的项目标识：彩色圆点 → **首字母色块**（.proj-ic）。
- 功能图标 emoji 清零（约 50 处）：🔒🌐⚠️ 徽章、👁 纵览、🗑⧉💾📦📭⤓☑ 等
  → 统一 stroke-2 线性 SVG（新增 `ICON_PATH` + `icon()` 工具），存储胶囊纯文字化（状态色已承担语义）。
- 术语残留：「点击成员卡片」→「点击成员」。

### 测试
- 单测 +16 条防回退守卫：Asana/收件箱/状态更新/Tab+Q/#E24D5C/珊瑚红/六个 emoji 图标不得再现；
  Ctrl+K 面板（openCmdk + KEYMAP）、proj-ic 必须存在；两条同步文案断言随改名更新。
- 全链验证：语法闸门 / 单测+集成 / QA 回归 / 双服务端对等 / 同步引擎 / 浏览器 E2E 全绿。

## [1.10.0] — 2026-09-25 · 全项目代码审查：修掉 19 条已验证问题（两条数据级 + 十一条正确性/性能）

### 背景
一次覆盖 Node 后端、PowerShell 后端与前端单文件的完整代码审查（三路并行 + 双代理交叉验证，
19 条发现全部经两个独立验证员确认存在）。修复按「数据完整性 → 正确性 → 性能」推进，
每修一条即补对等/回归测试，全链实跑通过。

### 数据完整性（critical）
- **账户表读取 fail-open 洗表风险**（Node）：`readAuthObj` 把 EBUSY/EPERM 等「读不了」当成空表，
  瞬时故障期间一张过期快照就能整片顶掉真实账户表（bootstrap 放行、查重与 tombstone 同时失效）。
  现在只有 ENOENT 算空表，其余照抛并 fail-closed（守卫回 500、tombstone 判定回 401）；
  PowerShell 版 `Protect-AuthWrite`/`Test-DeletedUid` 同口径对等修复。
- **PS 合并内核空数组塌缩成 null**：`return @()` 经 PowerShell 管道塌缩为 `$null`，
  嵌套空数组（`memberIds`/`subtasks`）落盘变 `null` → 页面白屏。`Convert-ToTree` 建树阶段
  同病（输入里的 `[]` 在合并前就变 null）。新增 `Emit-MergeValue` 逗号打包，全链路数组原样传出。

### 正确性（major）
- **PS/JS 语义漂移三连**：合并内核 `-eq` 比较规范化字符串（大小写不敏感）→ 改 `-ceq`
  （仅大小写不同的双方改动不再被静默吞掉）；`Select-Deterministic` 的 `-le` 随区域设置
  → 改 `[StringComparer]::Ordinal`；base 为空数组时 PS 版把整个数组二选一丢掉一方新增
  → 与 JS 对齐为按 id 合并。
- **合并对称性**：meta 合并改为确定性规则（先比 rev、再比 lastSaved、最后比规范化字符串；
  `lastSaved` 取两侧较大值而非当前时间）；数组顺序改为「base 骨架 + 新增按码元序」，
  交换 mine/theirs 结果一致——兑现合并内核文件头「任意机器算出同一结果」的承诺。
- **留底文件误判 uid**：`flowtask_data_*_conflict_/_corrupt_*.json` 与个人库同目录，
  被 uid 正则命中 → 共享盘出现幽灵 `users\` 目录、冲突副本被推上盘（Node/PS 两端同缺陷）。
  `PERSONAL_RE` 加负向前瞻排除（含旧版单文件 `flowtask_data_conflict_*` 形态）。
- **账户表写盘非原子**（双端）：专用端点直写最终路径，绕过 tmp+rename——全项目最不能坏的
  文件反而没防护。统一升级为原子替换（PS 复用 tmp+Move-Item，Node 复用 tmp+rename）。
- **PS 脱敏 fail-open**：账户表脱敏失败时 `catch {}` 静默、把含 salt/passHash 的整表原样下发。
  改为 fail-closed 回 500。
- **Node 并发写回滚竞态**：写失败回调把 `st.rev` 无条件打回，会抹掉排队期间他人已推进的版本，
  之后携带该版本的客户端绕过冲突检测被静默覆盖。现在只有记账仍停在本次推进值时才回滚。
- **热启用同步不补定时器**：界面里开启共享盘同步后，定期拉取定时器永不创建（只在启动时按
  当时的开关建），「自动采纳同事改动」失效直到重启。定时器生命周期移入同步引擎。
- **PS 连接泄漏**：请求处理抛异常（慢客户端 ReadTimeout、非数字 rev 头强转）时 TCP 连接不关。
  主循环补 `finally { $client.Close() }`，rev 头改 TryParse。
- **uid 大小写口径统一**（双端）：会话校验放宽（/i）而文件名白名单收紧，能建出
  「会话合法但个人库读写被拒」的死角账户。统一为大小写敏感，并在账户表写入侧校验新账户 id 格式。
- **登录失败限速**（双端）：`/api/session` 按 uid+来源 IP 计数，连续失败 5 次起指数退避
  （封顶 60 秒），成功清零。已知限制一并记录：口令证明本身仍是可重放的密码等价物
  （challenge 无一次性 nonce），默认 127.0.0.1 绑定下威胁有限，跨网段 HTTP 部署需自行加 TLS/反代。

### 性能
- **每请求全表读账户表**（双端）：鉴权热路径的 tombstone 检查改为带缓存读
  （mtime/大小变化 + 1 秒 TTL 失效，写路径主动作废）。
- **共享盘巡检预算**（双端）：定期同步单轮设 2 秒预算（文件之间检查），共享盘抖动时
  不再无限期占住请求路径；没做完的下轮接着做。
- **PS 请求解析**：body 逐块 `[byte[]]$chunk[0..($r-1)]` 装箱 + 头解析全缓冲 O(n²)
  → MemoryStream 直写 + 增量扫描。
- **前端渲染**：抽屉加「同一数据纪元内重复打开同一视图」去重守卫（此前一次交互重拼 2-3 遍）；
  行渲染的 users/tags 逐行 `find` 改 Map 索引；收起筛选面板不再整页重建；
  `enhanceA11y` 从整棵 document 收窄到本次真正重建的三个容器。
- **通知无界增长**：`DB.notifications` 此前只进不出（收件箱只显示 80 条），团队库场景下
  最终撑爆 localStorage 配额导致所有保存失败。与 activities 同策略截断到最近 500 条。

### 测试
- `sync_test.js` +6 条（73 → 79）：冲突副本/损坏隔离件/基线快照不得被当成个人库 uid。
- `qa_fix_regression.js`：A11/A11c 源码守卫更新到账户表原子写新形态（并断言直写旁路清零）。
- 新增 PS 合并内核冒烟场景（空数组保形、大小写差异记冲突、meta/顺序主客对称、空 base 双方新增都保留）。
- 全链实跑：语法 12 / 单测 317 / PS 冒烟 15 / QA 92 / 双服务端对等 83×2 / 同步引擎 79 /
  同步对等 79×2 / 两台机器集成 27 / 合并内核 33 / 合并端到端 18×2 / 浏览器 E2E 56，全部 0 失败。

## [1.9.0] — 2026-09-24 · 可见性只认成员名单；顺带逮到一个会静默丢写入的登出竞态

### 用户实测反馈
"我用管理员账号把某成员从一个项目里删了，登录那个成员的账号还是有那个项目，正常应该同步后去掉。"
盘上取证：那次移出**确实落盘了**（本地与共享盘的 `team\flowtask_shared.json` 名单里都没有他），
所以问题不在同步，而在可见性判定——旧写法是 `isAdmin() || memberIds.includes(ME.id)`，
而该成员的角色是**管理员**，管理员直接绕过成员名单。而且这个判定散在 8 处
（列表、侧栏、搜索、看板、路由守卫、角标计数），改一处漏七处。

### 改了什么
- 可见性收成唯一一处 `P.canSeeProject(p)`：**成员名单说了算，管理员也不例外**。
  把人移出项目 → 他刷新或等 30 秒同步后就看不到，符合直觉。
- 管理员的纵览能力改成显式动作：侧栏「项目」旁的 👁「显示全部项目」（默认关，
  只存本机偏好 `UI.prefs`，不写进任务数据，免得一次点击制造团队库冲突）。
  `PREF()` 只认真正的 `true`——手改 localStorage 写 `"1"`/`"true"` 打不开这个开关。
- 成员管理页与应用内帮助的权限文案一起改口径（原文写"管理员：全部权限…所有项目"，与新语义矛盾）。

### 挖出来的真 bug（比上面那条更要命）：退出登录会丢掉团队库那次写入
`pushStoreSvc()` 一次要推两份文件（个人库 + 团队共享库），旧写法"边推边现取"：
每次 `await` 回来后用 `if(!DB) break` 判断是否已登出，登出就收手。而 `logout()` 的顺序是
`flushAllSaves()`（同步进到推送函数、发出第 0 份请求）→ 清会话 → `ME = null; DB = null`。
结果第 0 份（个人库）推出去了、**第 1 份（团队共享库）整个被丢弃**，界面上毫无异常——
刚改的成员、刚加的任务，同事那边永远看不到。现在进函数就把两份待写内容与鉴权头
一次性快照（含 `X-FlowTask-Session`），推完不再依赖 `DB`/会话是否还在；
只有需要读写 DB 的 409 冲突处理在已登出时跳过并记下结论。`fetchStore` 增加 `pinned` 头参数供这条路径使用。

> 这条竞态在盘上有旁证：修复过程中某个只有本人可见的个人项目不知何时被共享进了团队库，
> 而本人的个人库变成空的——正是"改完立刻切账号"的操作序列。

### 测试
- 单测 +13 条（307 → 316，README 同步）：可见性 6 条行为断言（名单内 / 名单外 / 管理员默认
  不可见 / 👁 打开后可见 / 非法偏好值不得开启 / `visibleProjects` 与角标同口径）、
  2 条文本守卫（判定只剩一处且旧写法清零、👁 开关存在）、推送快照 2 条（旧 `if(!DB) break`
  不得复活、两份都用快照头发送）；`const P` 纳入测试标记区，并把上一轮"打桩 P"的假测试
  改成使用真权限对象。
- 浏览器 E2E +1 条 **M24**（55 → 56）：新建一个**管理员**账户 → 加进共享项目（他能看到项目与任务）
  → 用真实成员弹窗取消勾选保存 → 盘上核对名单已无他 → 他重新登录：可见列表与侧栏文字都不含
  该项目、👁 默认关 → 点 👁 后管理员能纵览 → 关回去并清场。
- 6 个既有场景（Z1/Z2/M1/M2/M5/M6）补上"我是谁"：它们过去靠"管理员能看到一切"侥幸通过，
  前一场景留下的登录身份其实不是它们假设的人。Z2 另修两处：清日期要连**子任务**一起清
  （首页把子任务算进"今天要处理"），起手 `maskRemoveAll()` 让 `--grep Z2` 单跑也成立。
- 全链实跑：语法 12 / 单测 317 / QA 92 / 双服务端对等 83×2 / 同步引擎 73 / 同步对等 79×2 /
  两台机器集成 27 / 合并内核 33 / 合并端到端 18×2 / 浏览器 E2E 56，全部 0 失败。

## [1.9.0] — 2026-09-23 · 把同事加进项目，从此真的能协作

### 事故现场
管理员建了 9 个项目、把三位同事都加进了 `memberIds`，同事登录后**什么都看不到**。
盘上取证的结论很直白：9 个项目全部躺在管理员自己的个人库文件里，
`flowtask_shared.json` 是 0 个项目、0 个任务。
`memberIds` 只是"这个项目的可见名单"，**它不会把数据搬进团队共享库**；
而服务端只把 `flowtask_data_<你的uid>.json` 发给你本人——同事登录后拿到的是
"她自己的空个人库 ⊕ 空的共享库"，于是界面上当然是空的。

这不是同步或账户的 bug，是**产品把人带进了一个没有任何反馈的坑**：
成员弹窗里只有一行埋在正文里的灰色小字提到"要协作请先共享给团队"，
点完保存还弹一句「项目成员已更新」——听起来像成功了。

### 修法：把"加了同事"这件事变成真的生效
- **保存成员时自动共享给团队**（用户选定：不再二次确认）。判定与副作用拆开：
  `decideAutoShare(proj, meId, isAdmin)` 是纯判定（`solo / already / archived / noperm / share`），
  `applyProjectScope` 负责物理搬库（项目 + 它的任务 + 它的通知 + 回收站里的对应条目一起走），
  `shareProjectIfCollaborating` 把两者串起来。**两个能改成员的入口都走同一条规则**
  （「管理项目成员」弹窗与「项目设置」弹窗）——只修一个门，另一个门照样能踩进去。
- 归档项目不被偷偷搬库（归档态改归属更容易出事），权限仍限定"创建人或管理员"。
- 成员弹窗与项目设置里都当场说明会发生什么；成功提示改成
  「已把「X」共享给团队：N 位成员登录都能看到它了」，并保留 6 秒撤销。
- 侧栏一眼可辨：🌐 团队共享 / 🔒 个人 / **⚠️ 个人项目但名单里有同事**（这正是过去静默失效的形状）。
- 迁移补齐：`scope` 缺失的老项目（单文件时代迁来的 8/9 个都是）显式补成 `personal`，
  否则界面读不到归属，"这项目共没共享"说不清；新建项目也直接带上 `scope:'personal'`。

### 顺带修掉的第二个静默失效：邀请通知寄不出去
`notify()` 从来不写 `projectId`，而 `splitStores` 是"有 projectId 才随项目走，否则归个人库"——
于是「邀请你加入项目」这类通知全部落在**发送者自己的个人库**里，收件人永远读不到。
现在 `notify()` 取 `param.projId`，或由 `taskId` 反查所属项目，把 `projectId` 补上，
通知就跟着项目落到对方读得到的那一份库里。

### 存量数据的一次性修复
把"名单里有同事但数据还在个人库"的 8 个项目连 46 个任务搬进团队共享库，
两份 `meta.rev` 都前进（否则还开着的旧页面回写会把修复整片顶回去），搬之前两份文件各留一份带时间戳备份。
那个只有本人的个人项目，保持个人库不动。共享盘上那份已核对：projects=8 / tasks=46。

### 测试
- 单测 +14 条（293 → 307）：`decideAutoShare` 的八种判定（含"两个入口都走同一条规则"
  与"文案里要说清自动共享"两条文本守卫）、`applyProjectScope` 会补齐创建人与操作者名单、
  `notify` 的 projectId 补齐与"没有线索时不凭空造"、迁移显式补 `scope`。
- 真浏览器 E2E +1 条（54 → 55）**M23**：走真实入口（成员弹窗勾选 + 保存）→ 内存里项目自动变共享 →
  到盘上取证共享库文件真的含该项目与该任务、且我的个人库里不再留第二份副本、
  邀请通知落在共享库里 → 换成同事账号登录，断言可见项目列表、项目下任务数、
  **侧栏里真的能看到**、以及未读邀请数。这条在旧代码上必然失败（自动共享那一步根本不存在）。
  附带记录一处测试驱动层的怪现象：跑到套件末尾对登录按钮发真实鼠标点击没有触发 submit，
  而 `elementFromPoint` 显示落点就是那个按钮本身（没有被遮挡、按钮也没禁用），
  同一条 `login()` 直接调用是成功的；场景因此保留 `requestSubmit()` 兜底（仍走应用自己的
  submit 处理器）并打一行 INFO 说明落点，真出现"按钮被东西盖住"时这条线索能立刻分辨出来。
- 全链实跑：语法 12 / 单测 308 / QA 92 / 双服务端对等 83×2 / 同步引擎 73 / 同步对等 79×2 /
  两台机器集成 27 / 合并内核 33 / 合并端到端 18×2 / 浏览器 E2E 55，全部 0 失败。

### 顺手补的一处虚假信心 + 一份面向使用者的说明
- **V3 场景标题写着「共享与收回」，实际只断言了"共享出去"，"收回"一个字都没测**——
  测试没在保护它却让人以为在保护，这类缺口比没测试更危险。补齐后 V3 现在真的走完一圈：
  创建人收回 → 确认框出现 → 两个库版本号都前进 → **盘上核对**共享库里那份真的消失、
  回到创建人自己的个人库文件 → 换管理员登录确认看不到它。收回的行为也在此明确：
  **移出同事不会自动收回**（自动把项目搬回某个人的个人库风险更大），收回只允许创建人本人。
- 多账户场景共用的登录封装提到骨架里成 `loginAs`（原来 M23 里有一份局部副本，
  抄一份只会越跑越脆）；真实点击 12 秒没进去就打"落点上是谁"的 INFO 再用 `requestSubmit()` 兜底。
- 新增 **`使用说明.md`**：面向"第一次拿到这个文件夹的同事"——新电脑三步起、账号谁来建/停用与删除的
  区别、共享文件夹怎么在界面里改、**个人项目与团队共享项目的归属表**（含"共享出去的文件
  对共享盘有读权限的人就能直接打开看"这条隐私边界）、备份与导出、常见问题速查表。README 顶部链过去。

## [1.9.0] — 2026-09-23 · 任务状态新增「已暂停」

### 为什么
项目里总有一类任务：没做完，但近期不推进（等对方回复、等活动、预算没批）。原来只有
待办 / 进行中 / 已完成三种，这种任务只能挂在「进行中」里 —— 于是首页天天催它逾期，
「今天要处理」被这些不动的事挤满，真正要做的反而看不见。参照 Asana 的 On hold 做成第四种状态。

### 行为定义（关键是"暂停"到底改变了什么）
- **不再被催办**：暂停的任务即使过期也不显示红色逾期，到期时间照常给，但按"昨天 / M月D日"的中性样式显示；
  看板卡片与列表行的标题降饱和 + 状态药丸用虚线边，一眼看出是"搁着"而不是"丢了"。
- **不占待办视野**：首页「今天要处理 / 未来 7 天」及其子任务、侧栏未完成角标、父任务"N/M"完成度都排除它；
  列表页把暂停单独收纳成一个可折叠分组（与活跃分组互斥，同一条任务不会出现在两处）。
- **不自动生成下一期**：重复任务在暂停期间不会到期就派生下一条。
- **老数据自愈**：读到不认识的状态值（比如从别的版本拷贝的库）会按 `completed` 归位到待办/已完成 ——
  否则那条任务在四种状态的列与分组里都不出现，就成了谁也都看不见的幽灵数据。

### 实现上的一个约束
四种状态只有 `STATUS_DEF` 一处真相源：列表分组、看板列、筛选药片、导出取值、颜色令牌
（`--st-paused-fg/bg/dot`）全部由它派生。单测加了两条守卫：一处是"界面里不许再出现第二份状态名清单"，
一处是"每个状态必须有对应的颜色令牌"——手写第二份清单正是这类功能最容易漏改的地方（漏一处就是某列空白）。

### 测试
- 新增 E2E `M21`：看板出现「已暂停」列；任务改成暂停后不再被催办逾期，且首页与角标都不计它。

## [1.9.0] — 管理员删除账户 + 修掉"账户表写入静默失败"这条链（三处真问题）

### 为什么要做
成员离职或账号开错了，现在只有「停用」。停用是"暂时不让登录"，但表里那条记录永远留着，
时间一长成员列表就成了废弃账户的坟场。删除必须和停用区分开：它是不可逆的，
所以确认框要先讲清楚"他的数据去哪了、别人的数据动不动"，而不是只问一句"确定吗"。

### 删除的设计（三条硬约束）
1. **个人库绝不 unlink**：改名留底成 `deleted_flowtask_data_<uid>_<时间>.json`，误删还能捞回来；
   留底失败就让整次删除失败，不允许出现"人没了、数据也没了"。
2. **写 tombstone（`auth.deleted`）**：只把账户从 `users` 里摘掉是不够的 —— 会话是 HMAC 自证的、
   不查表，被删的人手里那张旧会话还能继续读写。有了 tombstone，他的会话立刻 401。
3. **只走专用端点**：`POST /api/delete-user`（仅管理员会话），带最后一位管理员保护与"不能删自己"。
   别人库里的 `assigneeId` / `ownerId` 一律不改写（跨库改写风险更大），渲染层统一显示「已注销用户」。

### 顺带挖出来的三个真 bug（都是 M22 端到端才暴露的）
1. **专用端点改表后，文件里的 `meta.rev` 没跟着记账推进**：`/api/changepw`、`/api/resetpw`、
   `/api/delete-user` 只做了 `st.rev += 1`（内存记账），落盘的那份文本里 `meta.rev` 原地不动。
   于是客户端 GET 到的版本号永远比记账小 1，之后**任何一次整表回写必然 409，合并重试也永远追不上** ——
   表现出来就是"点了创建账户没反应，成员列表里始终没有他"，而且界面还显示"已保存"。
   现在三条端点统一走 `writeAuthObjWithRev`（两份服务端对等）：先把新 rev 写进文件，再落盘，
   最后才推进记账与指纹；写盘失败就整个不动，由调用方回滚留底。
2. **整表回写把"过期快照少一行"误判成删人**：上一轮为了防止误删，规定"表里少一行就 403"。
   但账户表读取本来就是脱敏的、每台机器都可能持着一张比服务端旧的快照，"少一行"绝大多数时候
   只是同事刚建的账户还没进我的表 —— 这条规则会把正常的建号也一起打死。改为**回写不做删除**：
   服务端把存量原样补回来（删人只能走专用端点，那才会留底 + 作废会话 + 写 tombstone）。
3. **tombstone 会被整表回写洗掉**：客户端手里那份脱敏表压根没有 `deleted` 字段，照原样落盘
   等于把"谁被删过"全忘了，下一张过期快照一推人就复活了。现在落盘前与存量做**并集**（只增不减）。

### 界面上不再允许"假装成功"
`saveAuth()` 改为返回 `Promise<{ok, err}>`，`pushAuthStore()` 把服务端给的 `err` 带出来。
建号必须等写入确认才宣布成功；没确认就回滚这一处内存改动、把原因显示在弹窗里（弹窗保持打开，可原地改完重试）。
改角色 / 停用 / 启用走同一个收尾函数 `commitAuthChange`：确认了才 toast 成功，否则回滚并如实报错。
纯浏览器（无存储服务）模式下本机缓存就是最终结果，照样返回成功，不会误报。

### 测试
- 双服务端对等新增 6 条：删号后"记账 rev 与文件 meta.rev 一致"、"按读到的 rev 递增回写不再撞 409"、
  "空表回写被补回存量（不清表）"、"少一行不被当成删除且 u_c 仍在"、"过期快照推回被删账户不会复活"、
  "复活动作没让其重新可登录"（Node 与 PowerShell 各 83 条全绿）。
- QA 回归把 A11 从"changepw 里写盘与算 hash 同源"升级为三条：统一落盘函数内同源、
  新 rev 先写进文件再序列化、三条端点都走它且全文件只剩一处真正写账户表（90→92 条）。
- 真浏览器 E2E 新增 `M22`（建号 → 确认框文案含留底与停用替代 → 删除 → 列表移除 → 换不到登录挑战），
  并把建号那一步做成自带取证：失败时把遗留弹窗数、点击前按钮状态、服务端/内存/DB 三处账户数、
  `_authHealthy` / `_saveState` / `_lastAuthPushErr` / rev 一次捞全。
- E2E 骨架加 `--grep`（逗号可选多个）：调试单个场景不再需要跑完 54 条。
  M22 就是这么抓出来的 —— 单跑时上一场景残留的弹窗会让 `.modal-mask .modal-ok` 命中底层那一只，
  于是"点了没反应"被误读成写入问题；现在场景自己先清场、所有点击都按内容锁定弹窗。
- 全链实跑：语法 12 / 单测 294 / 合并内核 33 / 双服务端对等 83×2 / 同步引擎 73 /
  同步双端对等 79×2 / 两台机器集成 27 / 合并端到端 18×2 / QA 回归 92 / 真浏览器 E2E 54，全部 0 失败。

## [1.9.0] — 冲突处理：实体级三路合并（不再整片二选一）

### 为什么要做
共享盘只有存储、不跑任何代码，没有协调者能裁定"谁的改动该保留"。原来的规则是
版本相同内容不同 → 双方留底 + 时间戳晚的一方整体胜出。结果是：两个人各自新建任务，
**后写的那个人会把前一个人的任务整片顶掉**（改动虽能去 `conflict\` 里捞，但界面上就是"我的任务不见了"）。

### 怎么做到的：客户端各自算，基线只存本机
新增 `flowtask_merge.js` / `flowtask_merge.ps1`（两份实现逐条对等）。每台机器在
`<DATA_DIR>\<库名>.base.json` 留一份"上次同步过的共同祖先"快照 —— **绝不上共享盘**，
因为盘上没有可执行代码，合并只能在客户端做。推送前比较 base / 我的 / 盘上的：
只有一边改 → 直接推或拉（同旧逻辑）；两边都改 → 按实体 id 三路合并，再逐字段合并，
**只有同一条实体的同一个字段都被改成不同值才算真冲突**，且用与主客无关的确定性规则裁决
（先比所属实体时间戳，再比规范化字符串），保证两台机器各自算出的结果相同 ——
否则合并本身会变成新的冲突源。合并结果同时写本机、共享盘、基线三方，让双方立刻收敛一致。

- 覆盖：`projects / tasks / notifications / tags / savedFilters / users` 按 id 合并；
  `comments / subtasks / activities` 在实体内部按 id 合并（两人给同一任务各加一条评论 → 都保留）；
  `memberIds / followers / tags` 这类标量集合取并集（两边都删才算删）；删除按"没改过才尊重删除"处理，
  对方改过就保留编辑并记冲突，避免误删同事的改动。
- 渐进生效、零迁移：没有基线时沿用旧的版本号规则，并在成功后落下基线，下一轮起才具备合并能力。
- 无法自动裁决时：整包（base/我的/对方的/合并结果 + 分歧清单）留底到 `conflict\<库名>_merge_<时间>.json`，
  账本记 `merged` 与分歧条数，界面能明确说"自动合并了什么、哪里需要你确认"。

### 顺带修掉的四个真 bug（都是两台真机器对撞才暴露的）
1. **拿不到锁就放弃推送**：同事正好在推时我这次改动被静默丢弃，一直躺在本地，直到下次本地写入才顺带带出去。
   现在按 1.5/4/9/20 秒退避重试，重试期间界面仍显示待同步；PS 版是串行模型，改为请求驱动的巡检重试 + 定期拉取。
2. **长开的机器永不拉取**：原来只在开机与登录时各拉一次，页面每 30 秒核对的其实只是本机旧数据，
   "自动采纳同事的改动"是空话。现在两端都定期从盘上拉（Node 30 秒定时器 / PS 借请求驱动）。
3. **PowerShell 变量名大小写不敏感**：`Merge-ArrayById` 里索引表 `$B/$M/$T` 与逐条变量 `$b/$m/$t`
   是同一个变量，第一行赋值就把索引表毁掉。另外 `@()` 与 `,$out` 混用会把数组多套一层，产出
   `"tasks":[[...]]`；`List[object]` 被 `@()` 包裹在 PS5.1 会抛"参数类型不匹配"；
   空数组被 `ConvertTo-Json` 写成 `null`，而页面碰到 null 数组会整片白屏。
   分别改为：索引表更名（不只差大小写）、返回持有者哈希表、冲突清单用哈希表累加、
   已知的数组键一律归一成 `[]`。

4. **本机缺文件时被误判成"需要合并"**：`needsMerge` 只挡住了"没有祖先"，没挡住"本机还没有这份"。
   于是新电脑登录时拿 `null` 去参与合并，把版本号又顶高一次 —— 拉回来的不是同事刚推的那一版
   （对等测试「拉回的版本号 = 7」直接报了 8）。修正为：三份齐全才算分歧，缺任何一方就按普通的
   推 / 拉处理，并在合并内核补 4 条对应断言。

### 测试
- 新增 `tests/merge_test.js`（33 条）：各改各的、同实体不同字段、评论并集、成员并集、
  同字段撞车、我删他改、双方都删、账户表、回收站分桶、`needsMerge` 判定（含三份任缺其一）、坏输入不炸；
  并专门断言**对称性**（交换 mine/theirs 必须得到同一内容）。
- 新增 `tests/sync_merge_e2e.js`（18 条，Node 与 PowerShell 各跑一遍）：起两个真实服务实例 +
  两个数据目录 + 一个共享盘，两人各建任务并各改同一任务的不同字段，断言盘上 `t0,ta,tb` 三条都在、
  A 改的负责人与 B 改的标题同时保留、双方最终收敛到同一份内容。
- 语法闸门纳入 `flowtask_merge.js` 与两个新测试（9→12 项）。
- 全链实跑：语法 12 / 单测 293 / 合并内核 33 / 双服务端对等 124 / 同步引擎 73 /
  同步双端对等 79×2 / 两台机器集成 27 / 合并端到端 18×2（Node 与 PowerShell）/
  QA 回归 90 / 真浏览器 E2E 52，全部 0 失败。


## [1.9.0] — 账号只由管理员开通（阶段 2）+ 修好一处一直存在的死链

### 修复：个人设置的「修改密码」在存储服务下必然失败（既有缺陷，非本次引入）
自 v1.6 起账户表读取脱敏（页面拿不到 `salt`/`passHash`），但该表单仍在本地做
`verifyPassword(ME, oldPw)` 比对 `ME.passHash` —— 在服务模式下它是 `undefined`，
于是**任何人改密码都会被告知「当前密码错误」**，而已经做好、且被对等测试覆盖的
`/api/changepw` 前端从来没调用过。现在改密一律走服务端，错误提示也区分开
（当前密码不正确 / 会话失效 / 服务未响应，不再一句"当前密码错误"糊住三种情况）。

### 新增：账号只由管理员开通
- 配置项 `openRegistration`（默认关闭）。**服务端账户表守卫是真正的约束**：表里已有活跃账户时，
  没有管理员会话就不得新增账户；两个豁免——① 表为空的首启 bootstrap（否则没人能开第一个号），
  ② 显式 `openRegistration:true`。登录页据 `/api/ping` 下发的标志收起注册入口，
  改为提示「账号由管理员开通；忘记密码请找管理员重置」——那只是少露一条必然失败的路，不是安全边界。
- 新建成员账户：初始口令改为**随机生成**（10 位、四类字符、排除易混的 0/O/1/l），可重生成、可一键复制，
  创建后自动 `pwMustChange`。原来的写死 `123456` 意味着全组共用同一个初始口令。
- 成员列表新增「重置密码」，走新的 `POST /api/resetpw`（仅管理员会话；非管理员 403、未知账户 404、
  哈希格式不合法 400）；重置后旧口令立刻失效并要求对方首登改密。本人改密成功后该标记自动清除。
- 首登引导：用初始口令登录会弹「先改一下密码」，**可关闭不锁人**（把人锁在门外只会让人绕回 Excel），
  成员列表用「待改密」标显示谁还没换，改完即消失。

### 修复：order 相同的两条任务，键盘排序按了没反应（既有缺陷）
`Alt+↓` 直接交换两条的 `order`；而同一毫秒创建的任务 `order` 完全相同，交换相等值 = 什么都没发生。
两处治：① 键盘排序遇到相等值时强制拉开一个名次；② 新建与演示数据的 `order` 改为组内递增落位，
从源头不再产生并列值。
这是给 M2 去掉「样本 order 相同就跳过」的假绿之后被暴露出来的——原断言把真缺陷当成了跳过条件。

### 测试
- `tests/server_parity.js` 新增 26 条：匿名新增账户被拒 / 管理员会话可建号 / 首账户 bootstrap 仍可用 /
  resetpw 的鉴权与失败分支 / 重置后旧口令 401 新口令 200 / 本人改密清标记，Node 与 PowerShell 逐条一致。
- E2E 新增 M17（界面建号→待改密标→新人登录被引导→错密码被拒→改密成功标记清除）、
  M18（重置后旧口令立刻失效并重新要求改密）、M19（关闭自助注册时登录页不露注册入口）。
- 单测新增 12 条（`nextTaskOrder` 落位与并列值防护、初始口令随机性）。
- M2 从「DOM 前两行 + 比较具体 order 值」改为「按被测函数自己的口径取样本 + 只验排序位置」，
  并去掉静默 skip；此前它偶发失败（四轮里挂两次），修完连续两轮 52/52 全绿。
- 实跑：语法 9 / 单测 293 / 双服务端对等 124 / 同步引擎 73 / 同步双端对等 79×2 /
  两台机器集成 27 / QA 回归 90 / 真浏览器 E2E 52（连续两轮），全部 0 失败。



## [1.9.0] — 同步文件夹可在界面里改（不必再碰配置文件）

### 为什么需要
共享盘按部门、按账户的权限不一样，同一个写死的路径不可能适合所有人。原先要改只能
重命名 example、手写 UNC 路径、重启服务——而且中文路径经 cmd 设环境变量还容易被编码吃掉。

### 新增
- 数据管理页「共享盘同步」卡片：看当前状态与路径、改路径、停用/再启用、立即同步一次。
  只有管理员能改（服务端同样强制，绕过界面也没用）。
- `GET/POST /api/sync/config`（两端对等）：管理员专属；写入用与数据文件同一套
  「临时文件 + 原子改名」，只覆盖白名单键，用户手加的备注字段原样保留。
- 引擎支持热切换（Node `reconfigure` / PS1 `Set-ShareConfig`），改完立即生效，无需重启。
- **先探后确认**：`validateOnly` 通道做一次无副作用探测（随后一律退回原设置、绝不落盘），
  路径不行就直接说明，不让用户确认完切换才发现白填一场；探测通过才弹「切换同步文件夹？」确认框，
  因为换路径有真实后果（新位置可能有别人更新的数据，冲突时双方留底）。
- 校验不通过时：拒绝、不落盘、运行时退回原路径、并且绝不凭空创建目录。
- `shareDir`（配置的意图）与 `activeShareDir`（当前生效值）分开返回。

### 过程中修掉的真问题
- **PowerShell 路径表示不一致**：启动时用 `Get-Item .FullName` 规范化，热切换时忘了做同一件事，
  同一个目录会以 8.3 短名/长名两种字符串出现，状态显示与实际写入路径可能不是同一个东西。
  抽出 `Resolve-ShareDir` 让两条路径走同一规范化。
- **停用同步会连带把路径抹掉**：运行时停用后 `ShareDir` 为空，界面重绘时输入框也空了，
  「保留路径便于再启用」在界面上根本不成立 → 拆成意图与生效值两个字段。
- 测试里两处 `path.resolve` 比较目录不可靠：`fs.realpathSync`（libuv 自算）**不**展开 8.3 短名，
  要用 `fs.realpathSync.native`（走 `GetFinalPathNameByHandle`）→ 抽出 `sameDir()`。
- 一条断言用 `process.env.FLOWTASK_DATA_DIR || ''` 拼路径读配置：harness 只把该变量传给子进程，
  本进程里它是空的，于是**读到了仓库里真实的 `flowtask_config.json`（生产配置）**。只读未写，
  但这类越界读取已从测试里移除，改为一律经服务端断言。

### 测试
- `tests/sync_parity.js` 新增场景 16（25 条 ×2）：管理员专属鉴权、坏路径拒绝且退回不落盘、
  validateOnly 探测不改设置、切到新位置后镜像真的写进去、停用保留意图、只传开关即可重开。
- E2E 新增 M20：从界面填坏路径→拒绝且不建目录→填好路径→确认→卡片转「已连通」且磁盘上出现镜像→
  停用→输入框仍保留路径→不重敲即可再启用→收尾停用，并校验顶栏同步签与新配置一致。
- 单测新增 14 条（`shareConfigView` 取态分离 + `genInitialPassword`）。
- 实跑：语法 9 / 单测 286 / 双服务端对等 98 / 同步引擎 73 / **同步双端对等 79×2** /
  两台机器集成 27 / 真浏览器 E2E 49，全部 0 失败。



## [1.9.0] — 新增数据导出（表格 CSV / Markdown 摘要 / JSON 备份）

原来只有一个「导出数据 (JSON)」，吐的是整份系统数据——那是给系统还原用的，不是给人看的。
同事要的是"把这个项目的任务表发我一份"，所以按**用途**分三条出口，进同一个弹层：
范围 → 内容 → 格式，边选边预览行数、列数与文件名。入口有三处：项目页工具条图标（Asana 的位置）、
我的任务、数据管理页。

### 表格 CSV（主用途）
- **UTF-8 BOM + CRLF**：没有 BOM 中文在 Excel 里就是乱码，这是中文环境导出最容易翻车的一点。
  BOM 用 `String.fromCharCode(0xFEFF)` 生成而不是字面字符——字面 BOM 看不见，编辑器一存盘就丢。
- **防公式注入**：标题/描述是同事可写的自由文本，以 `=` `+` `-` `@` 开头的单元格会被 Excel 当公式执行；
  导出时统一加前导单引号（纯数字豁免，否则评论数会变成文本）。
- 引号/逗号/换行按 RFC4180 转义；描述抽成纯文本并还原 HTML 实体（双重编码不被二次解码）。
- 18 列可选、默认勾 10 个常用列；列顺序恒按定义顺序，与勾选先后无关。
- 「当前筛选结果」直接复用列表页的 `applyFilter`，保证**看到什么导出什么**，不另写一套筛选口径。
- 取数一律走 `P.visibleProjects()`：成员看不到的项目也不该被 TA 导走。

### Markdown 摘要与 JSON 备份
- Markdown 按项目分组，`- [ ]/- [x]` 复选样式 + 负责人/状态/优先级/截止日期，逾期就地标注，
  优先级只标非「低」——贴进钉钉、邮件、文档都能直接读；提供「复制 Markdown」与下载 `.md` 两条路。
- JSON 备份与报表分开表述，避免误以为是"给人看的导出"；下载路径统一到一个 `downloadText`。

### 三处由测试逼出来的真问题（都已修）
1. **展开子任务却不给归属列**：勾了「展开子任务」后 CSV 里子任务行与任务行无法分辨——
   现在勾选时自动带上「类型」「父任务」两列，取消时撤掉（E2E M15 抓到）。
2. **测试标记区 BEGIN/END 错位**：`PRIO`/`prioName`/`ROLE_NAME` 那一段只有 `TEST-END` 没有 `TEST-BEGIN`，
   整段落在测试沙箱之外；此前没人从沙箱里用 `prioName` 所以一直没暴露。已把三处标记配平（顺带修正 990 段未收尾）。
3. **导出被拒绝时不能关窗**：列全取消、行数超过 20000 上限时，直接拒绝并在汇总区写明原因——
   给一个空文件或默默截断，用户会拿着少行的表去汇报。

### 测试
- 单测新增导出内核断言若干（BOM/CRLF/转义/注入豁免/纯文本/时间戳/逾期口径/列唯一性与默认集/
  子任务展开与状态独立于父任务/Markdown 分组），合计语法 9 / 单测 272。
- E2E 新增 M14（真浏览器里生成 CSV：验 BOM、中文表头顺序、引号逗号转义、`=1+1` 被加前导引号、文件名带日期、
  逾期列口径）、M15（展开子任务改变产物 + 列全取消被拦下且弹层不关）、M16（Markdown 分组与复制态）。
- 全链实跑：语法 9 / 单测 272 / 双服务端对等 98 / 同步引擎 73 / 同步双端对等 54×2 /
  两台机器集成 27 / QA 回归 90 / 真浏览器 E2E 48，全部 0 失败。

## [1.9.0] — 局域网多用户升级 · 阶段 1（共享盘同步，Node + PowerShell 双实现）

### 修复：同步本是自动的，但界面让人以为必须点一下
- 反馈缺口：状态签此前只靠 30 秒轮询刷新，保存完顶栏一动不动、点一下才跳，于是读成「要点才同步」。
  实测两端写入后镜像都会自己跟上（Node 约 3 秒去抖、PowerShell 回包即推，全程不调 `/api/sync`）。
- 现在保存成功即登记过渡态，顶栏立刻转「⏳ 同步中…」，并在 3.6 秒与 9.6 秒各自动刷新一次；
  服务端回报的 `lastSyncAt` 晚于本次保存就立即收掉过渡态，不干等轮询。
- PowerShell 版不回报排队数，所以过渡态由客户端自己记（`syncStatusView(info, awaiting)` 的第二参数），
  不依赖 `info.pending`。配置问题（`blockReason`）优先于过渡态——共享盘打不开时绝不显示「同步中」。
- 提示文案点明「不用点它——点一下也可以立刻催一次」；账户表变更（建号/改密）走同一条过渡态。
- 登出与会话失效的五条路径一并清掉过渡态，不残留上一位用户的状态。
- `syncStatusView` 的 `awaiting` 分支补 4 条单测与 1 条 E2E（M13：真实保存后顶栏自动转同步中，不点任何按钮）。

### 修复：路径打错时会凭空建目录并谎报「已同步」
- 现象：`shareDir` 指向一个不存在的**本地**路径时，`ensureDir` 会把这条路径整建出来，数据静默落进
  一个谁都不知道的空目录，而顶栏显示「已同步」——这是全套设计里最坏的一种假象（用户以为资料在共享盘）。
- 两道闸：① 启动时 `checkShare` / `Test-ShareDir` 要求「已存在的目录 + 写得进」（用独占创建的探针文件验），
  不过就把原因记进 `blockReason`；② 每次同步前复查根目录是否还在，不在就 `hold`，**引擎绝不创建共享盘根目录**。
- 顶栏把「配置本身有问题」和「共享盘临时掉线」拆成两种文案：前者是 `⚠ 共享盘打不开` 并给出下一步
  （先在资源管理器里能打开、检查写权限、改完重启），后者才是 `⚠ 共享盘断开`。重连治不好前者，不能混为一谈。
- 恢复可写后 `blockReason` 自动清除，不会挂着旧错一直报红。
- 顺带修掉 `schedulePush` 里 `unref` 从未生效的一处（在 `delete` 之后才取定时器）。

### 新增：用配置文件开启同步（不需要命令行）
- `flowtask_config.json`（放在**数据目录**，默认即 FlowTask 文件夹）+ `flowtask_config.example.json` 模板。
  优先级：命令行参数 / 环境变量 `FLOWTASK_SHARE_DIR` > 配置文件 > 默认关闭；`"syncEnabled": false` 可临时停用而保留路径。
- 刻意读数据目录而不是程序目录：测试会把 `DATA_DIR` 重定向到临时目录，这样跑回归**不可能**读到真机配置、
  把测试数据写进生产共享盘。这条隔离性有对等场景守住（配置镜像到临时共享目录才算通过）。
- 实际配置文件已在 `.gitignore` 里（含机器相关路径），只入库模板。

### 修复：PowerShell 里「读不到」会被误判成「没有这份文件」（会导致覆盖）
- `Read-OrMissing` 原先用类型化 catch（`catch [System.IO.FileNotFoundException]`）区分缺失与读失败，
  但 PowerShell 会把 .NET 方法异常包成 `MethodInvocationException`，类型化 catch 匹配并不稳定——
  一旦「读不了」被判成「不存在」，同步就会拿本机旧数据去覆盖共享盘上更新的那份。
- 改为显式剥 `InnerException` 取真实类型，并加一条前置判定：路径被**目录**占住 = 读不了（不是缺失）。
  Node 版同步加上同样的目录判定，两端错误语义对齐（`is-a-directory` / `share-read:*`）。
- 这条由新增的**双服务端同步对等测试**抓到：同一场景 Node 回 `hold`、PowerShell 回 `error`。

### 新增：PowerShell 版同步引擎 `flowtask_sync.ps1`（与 `flowtask_sync.js` 逐条对等）
- 决策表、目录契约、跨机锁（`FileMode::CreateNew` == Node 的 `wx`，60s 超时抢占）、
  冲突双方留底、推前复核远端版本、坏文件不覆盖不拉回——与 JS 版同规则同文案。
- 接入 `flowtask_server.ps1`：`-ShareDir` 参数（也可用 `FLOWTASK_SHARE_DIR`）→ dot-source 引擎 →
  开机 `Invoke-BootSync` 先拉后服务 → `POST /api/session` 里 `Invoke-LoginSync` →
  新增 `GET/POST /api/sync` → 写成功后回包再推。
- **串行模型的差异处理**：PowerShell 版是单线程串行处理请求，没有 JS 的 3s 去抖定时器，
  改为「响应已发出、连接已关闭之后」在主页循环里推一次（`$script:syncPendingPush`），
  这样保存请求不会被 SMB 往返拖住，与 Node 的「先回包后去抖推送」观感等价。
- 拉取改写本地文件后由 `Invoke-OnLocalChanged` 作废 `$script:fileState` 缓存（服务端覆盖引擎里的空实现），
  避免拿旧指纹比对产生假冲突或漏判真冲突。

### 新增：顶栏同步状态与「立即同步」
- 顶栏新增一枚状态签：`☁ 已同步 14:03`（绿）/ `⏳ 同步中 N 项`（蓝）/ `⚠ 共享盘断开`（红）。
  **未配置共享盘时这枚按钮完全不出现**，默认模式下的界面与今天一致。
- 点击即手动同步（`POST /api/sync`），完成后按结果播报：推上去几项、拉回来几项、
  有冲突时明确说明「双方都已留底，页面显示时间更晚的那一份」。
- 断开态文案先讲清「本机数据仍在正常保存」，再附最近一次失败原因，避免看到红签就以为丢数据。
- 登出 / 会话失效（三处拆解路径）都会收回这枚指示，不会残留上一位用户的同步状态。
- 每 30 秒核对一次：刷新状态签，并在共享盘上有更新时自动采纳。
  这顺带补掉一个老缺口——此前远端版本只在「切回标签页」时核对，多人使用时同事的改动要等切页才看得见。
- 服务端在登录后把共享盘版本覆盖到本机时，页面按 `changedLocal` + 版本号比对重读进内存，
  不会继续显示旧数据（复用既有 `pullStore`，与多标签页同步同一条通路）。
- `syncStatusView()` 做成纯函数并纳入单测（7 条），含「未配置不显示」「断开不得显示成已同步」两条守卫。

### 测试
- 新增 `tests/sync_parity.js`：同一套 43 条同步场景分别跑两个实现，逐条断言一致——
  鉴权边界、账户表/个人库镜像、状态可查、新鲜锁挡住写入、超时锁抢占、无改动不产生多余写入、
  真冲突双方留底且晚者胜并通知页面重读、坏远端不覆盖不拉回、读失败保守不动并标记不可达、
  **开机续传的隐私边界（绝不把同事的个人库拉到本机，个人库在登录时才恢复）**、关掉开关后回退纯本机。
- `tests/_ps1_parse_check.ps1` 重写：自动覆盖仓库根目录全部 `.ps1`（新文件无需再改），
  并新增一条闸门——含中文的 `.ps1` 必须带 UTF-8 BOM，否则 PowerShell 5.1 会按 GBK 读成乱码。
- 实跑：语法 9 / 单测+守卫 226 / 双服务端对等 98 / 同步引擎单测 73 / **同步双端对等 54×2=108** /
  真浏览器 E2E 45 / QA 回归 90 / PS1 解析 3 个脚本，全部 0 失败。


### 新增：本地优先 + 共享盘同步（Node 侧内核，开关默认关闭）
- 新文件 `flowtask_sync.js`：共享盘同步引擎。核心是纯决策函数 `decideAction(本机元信息, 共享盘元信息)`
  → `noop | pull | push | conflict-push | conflict-pull | invalid-*`，不碰文件系统，可独立单测。
- 三条不容妥协的规则：**①** 共享盘读失败区分 `ENOENT`（这份还不存在，可推）与其它错误
  （网络断开/无权限，不知道对面是什么 → 绝不动，绝不覆盖）；**②** 版本比 `meta.rev` 高者胜，
  rev 相同但内容哈希不同 = 真冲突 → **双方都先复制到 `conflict\` 再定胜负**，任何情况不静默丢数据；
  **③** 写 `team\` 前抢 `locks\<name>.lock`（独占创建 + 60s 超时抢占），推之前再复核一次远端版本。
- 目录契约：`team\`（账户表 + 共享库）/ `users\<uid>\`（每人一份，唯一写者就是本人）/
  `locks\` / `conflict\`。`flowtask_sync.js` 负责路径映射与落盘。
- 服务端接入 `flowtask_server.js`（全部受 `FLOWTASK_SHARE_DIR` 控制，**未配置=纯本机、行为与今天一致**）：
  开机 `pullOnBoot` 续传本机服务过的那些人的数据；`POST /api/db` 成功后 `schedulePush` 去抖推送（不阻塞响应）；
  `POST /api/session` 签发会话时 `loginSync` 定向拉回该账户的个人库（换电脑首次登录即拿回资料）；
  新增 `GET/POST /api/sync` 查询状态与手动「立即同步」。拉取改写本地文件后回调 `onLocalChanged` 作废
  服务端 `loadState` 的 rev/hash 缓存，避免假冲突/漏判真冲突。

### 测试
- `tests/sync_test.js`：57 条。临时目录假扮共享盘，覆盖决策表全分支、推拉落盘、换电脑登录拉回、
  真冲突双方留底、跨机锁抢占、共享盘读失败保守不动、账户表同步、开关关闭时完全惰性、去抖推送。
- `tests/sync_integration.js`：27 条。起两个真实服务端实例 + 两个数据目录 + 同一共享盘，
  端到端验证「A 机建账号建数据 → B 机登录拉回 → B 机改动推上去 → 冲突时双方留底且页面被通知重读」。
- `tests/syntax_check.js` 纳入 `flowtask_sync.js`、其单测与共享盘冒烟工具（语法闸门 4→7 项）。
- `smoke_share.js`（手动工具，不进自动化链）：16 条打到**真实 SMB 共享盘**跑通，实测
  「本机保存 → 共享盘可见」约 3.1~3.4 秒（含 3 秒去抖）。跑完只清理自己登记过且开跑前不存在的路径，
  目录非空则保留；共享盘原有数据一律不碰，目标非空需显式 `--allow-nonempty` 才肯运行。
- 回归（本次改动后实跑）：语法 7 / 单测+守卫 211 / 双服务端对等 98，全部 0 失败。
  真浏览器 E2E（42）与 QA 回归（90）在提交后单独复跑确认，见下一条。

### 已知未完成（诚实记录，不是 bug）
- 顶栏状态签、手动同步、断连降级与 30 秒自动采纳均已接入；两种服务端行为对等。
  默认不配置共享盘时，界面与数据行为都和旧版一致（这枚按钮不出现）。
- 阶段 2（管理员开号、去自助注册、首登强制改密）、阶段 3（部署脚本共享盘检查）未开始。
  架构与分期：本地库 + 后台同步引擎 + 三路合并内核，分三期落地。

## [1.8.1] — 2026-09-08

### 新增：子任务拖拽排序 + 首页子任务完成圆圈


**1. 子任务可拖拽排序**
- 任务抽屉里的子任务列表支持上下拖动排序：拖到目标行上/下方（有品牌色落点指示线），
  松手即重排并落盘；仅可写成员可拖，只读访客不显示拖拽。
- 与任务列表/看板的拖拽同一套交互语言（dragover 落点指示 + drop 重排）。

**2. 首页子任务行补完成圆圈**
- 首页「今天要处理 / 未来 7 天到期」里，主任务行有完成圆圈而子任务行没有——
  现在子任务行首也有同款圆圈：点击直接完成/恢复子任务（走 toggleSubDone，
  重复任务会照常生成下一期），点行的其余部分仍是打开子任务详情。
- 完成后该子任务从首页卡片消失（与「已完成子任务不出现」的规则一致）。

### 测试
- E2E：M9 首页圆圈点击完成并落盘/消失；M10 抽屉拖拽排序（A/B/C → B/A/C，
  DOM 与数据一致）。2 条静态守卫。
- 五层全绿：语法 4 / 单测+守卫 209 / 双服务端对等 98 / E2E 42。

## [1.8.0] — 2026-09-07

### 新增：首页「今天要处理 / 未来 7 天到期」同时显示子任务

- 首页两张到期卡片不再只看主任务：可见且未归档项目里、**未完成且有截止日期的子任务**
  一并纳入——逾期子任务计入「今天要处理」的已逾期分组，今天到期的进今天分组，
  未来 7 天的进「未来 7 天到期」卡片（按日期排序）。
- 子任务行带「子任务」标记并缩进显示所属项目与父任务，点击直接打开子任务详情抽屉。
- 已完成的子任务不会出现在首页。
- 分组计数（已逾期 N 项 / 今天到期 N 项 / 卡片角标）均合并子任务后统计。
- E2E M8：三种子任务（今天未完成 / 3 天后 / 今天已完成）在首页的可见性与点击行为。

## [1.7.5] — 2026-09-07

### 对标 Asana 的三处体验优化

**1. 活动记录可折叠**
- 任务与子任务抽屉的「活动记录」平时只显示 **创建记录 + 最近 3 条**，底部给出
  「显示全部 N 条动态（当前只显示创建记录与最近 3 条）」；点开变「收起动态」。
- 展开状态在会话内按任务记忆：发评论、改状态会重绘抽屉，但不该把用户刚展开的
  列表弹回折叠态。活动少于等于 4 条时不出现折叠入口（没必要）。
- 渲染上限仍是最近 30 条（与活动记录 200 条存储上限分离，长历史任务不拖抽屉）。

**2. 切换任务时回到顶部**
- 之前只有"同一条任务重绘"会保留滚动位置，切到**另一条**任务时视口停在上一条的
  阅读位置，容易误以为内容没加载。现在：同一条保留、换任务一律回顶部。

**3. 快速添加的默认项目跟随上下文**
- 优先级：**显式传入 > 当前所在项目 > 上一次创建任务的项目 > 第一个项目**。
- 在项目页里按 `Tab+Q`（以及顶栏「＋ 添加任务」），默认项目就是当前项目，不用再重选；
- 在首页/我的任务等不在项目上下文里呼出时，用上次创建任务的那个项目；
- 「上次创建的项目」存在本机 UI 偏好里（与筛选、项目顺序同类），**不写进任何数据文件**，
  因此不会因为记一个默认值就去重写团队共享库、制造版本冲突。

### 测试
- 新增 E2E 场景文件 `tests/flowtask_e2e_ux2.js`（骨架接入，便于按批维护）：
  M6 活动折叠/展开/会话记忆 + 切换任务回顶部；M7 默认项目的两条回落路径。
- 11 条纯函数单测（foldActivity 的边界与顺序、quickAddDefaultProject 的优先级与失效 pid 兜底）、
  3 条静态守卫（两抽屉都绑定折叠、两处滚动逻辑、默认项目不再固定第一个）。
- 五层全绿：语法 4 / 单测+守卫 207 / 双服务端对等 98 / E2E 39。
- 加固 Z1（偏好设置）的一处测法：它取「改动前的版本号」时没有等待在途的防抖推送落地，
  于是把上一条用例排队的写入误算成「改偏好顶高了任务数据版本」而偶发失败。现在先等
  待保存态空闲 + 防抖窗口结束再取基线；断言要守的规则（偏好只写 localStorage）不变。

## [1.7.4] — 2026-09-07

### 修复：点「添加」后弹窗不关闭

v1.7.0 为了"批量连续录入"把快速添加改成了创建后不关窗，这是错判——主按钮的语义就是
"完成这次添加"。现在改回：**点「添加」（或标题里按 Enter）创建后弹窗立即关闭**；
需要连着录多条走显式的第二条路径——点「＋ 添加并继续」或在标题里按 `Shift+Enter`，
此时创建后保留弹窗、清空标题、项目/负责人/日期/状态等上下文原样留着。

- **添加子任务**弹窗同样处理（主按钮创建即关闭 + 「＋ 添加并继续」+ Shift+Enter），
  并顺带补上"默认指派给自己"（此前只有任务有）、@负责人解析对非项目成员不再命中；
  手动选过人之后解析与默认值都不再覆盖。
- **其余同类弹窗逐一核对**：新建项目、新建成员账户、发布状态更新、项目设置、
  管理项目成员、创建标签、导入/删除类确认框——成功路径都正确 `return true` 关闭，
  校验失败才保持打开；未发现同类缺陷。
- 应用内帮助（?）的快捷键表补 `Shift + Enter` 一行。
- 测试：U5 用例按新契约重写（主按钮关闭 + 显式连续录入保持打开），新增 3 条静态守卫。

## [1.7.3] — 2026-09-05

### 优化：评论与动态的时间改为「按日期追溯」

此前超过 24 小时的时间一律显示「N天前」这类相对表述——要追溯
「这条评论是围绕哪次评审说的」就得自己心算换算，时间越久越没信息量。新策略：

| 距今 | 显示 | 悬浮提示 |
|---|---|---|
| < 1 分钟 | 刚刚 | 完整时间戳 |
| < 1 小时 | 10 分钟前 | 完整时间戳 |
| < 24 小时 | 3 小时前 | 完整时间戳 |
| 昨天 | 昨天 16:45 | 2026-09-04 16:45 |
| 今年内 | 8月26日 16:45 | 2026-08-26 16:45 |
| 跨年 | 2025年12月3日 | 完整时间戳 |

- 覆盖全部时间显示位：任务/子任务**评论**、**活动记录**、收件箱通知、搜索下拉、
  项目概览的状态更新、回收站「删除于」；「（已编辑）」也补上了编辑时刻。
- 每个时间点带 `title` 悬浮完整时间戳（到分钟），核对细节时不用换算。
- 新增 `fmtDateShort()` / `timeTitle()` 两个纯函数并入可测区：9 条单测覆盖
  边界（刚刚/分钟前/小时前/昨天/日期/跨年/非法输入），3 条静态守卫防回潮。
- 顺带修掉 M5 用例在页内 console 暴露的一个真实竞态：`pushStoreSvc` 在 `await`
  期间若已登出（`DB` 置空），收尾会 `DB.meta` 抛未捕获异常——现在每轮循环与
  收尾处都重新判空（E2E 页内 console 有异常会被打印，这条是它抓出来的）。

## [1.7.2] — 2026-09-05

### 新增：新建任务默认指派给当前用户

- 快速添加的「负责人」下拉默认选中**我（我）**——在所选项目成员里包含我时生效；
  标题里 `@某人` 的解析仍会覆盖默认值，用户手动选择后两者都不再覆盖。
- 我不是所选项目成员时保持「未指派」默认不变（如管理员在他人项目中代录）。

### 修复：回收站「清空」对非管理员不可用

- v1.6.0 曾因越权风险把「清空回收站」收紧为仅管理员——但这导致普通成员
  （含管理员自己）完全用不上该功能。现改为**按管理权分范围**：
  - 管理员：一键彻底删除全部（含共享库）；
  - 其他用户：只彻底删除**自己有管理权的条目**（个人库条目 / 自己创建的
    共享项目条目），确认框明确列出影响范围与保留数量；共享库中他人的
    条目仅管理员可清空。
  恢复与清空共用同一管理权判据（可恢复 ⇔ 可清空），权限语义自洽。

### 确认：新电脑部署流程

- 拷贝文件夹 → 双击 `环境检查与部署.bat` → 启动器每次运行都会按**本机路径**
  重建「文件夹内 + 桌面」的 `FlowTask.lnk`（CreateShortcut 覆盖式写入），
  因此 bat 跑完后快捷方式即直接可用，无需任何手工步骤。

## [1.7.1] — 2026-09-05

### 新增：一键环境检查与部署（迁移到新电脑）

- **`环境检查与部署.bat`**：拷贝文件夹到新电脑后双击一次即可完成部署——
  依次检查核心文件 / 运行环境（PowerShell 必需、Node 与 Edge 可选）/
  账户表与个人库与共享库数据文件 / 端口 5178 占用 / 自动备份目录，
  然后交给一键启动器完成「启动服务 → 按新机路径重建快捷方式 → 打开浏览器」。
  中文检查逻辑在 `deploy_check.ps1`（UTF-8），bat 本体保持 ASCII，
  任何代码页都不会乱码。

### 整理：文件夹清理（不影响正常使用）

- 归档移除（历史可从 git 追溯，物理副本已另行留底）：
  - `flowtask_data_conflict_*.json` ×6 —— 历史冲突留底副本（数据已在新库中）
  - `make_icon.py` —— 已被 `make_icon.js` 取代
  - `重建快捷方式.vbs` —— 启动器每次运行都会自动重建快捷方式，功能重复
  - `应用文件夹图标.bat` —— 一次性文件夹图标设置（desktop.ini 已生效）
  - `e2e-shots/` —— E2E 截图证据（每次测试自动重新生成，已加入 .gitignore）
  - `flowtask_data.json` —— 旧版单文件（见下）
- **旧版单文件里的未认领数据已先安置**：清理前核查发现 `flowtask_data.json`
  中还留有一个旧共享项目（创建人当时未登录，迁移机制按创建人分配、一直在等本人
  登录认领）——已把它连同任务复制进创建人本人的个人库并重启服务，旧文件此后仅剩
  演示项目旧副本，随本次整理一并归档；万一需要，物理副本与 git 历史均可找回。
- 整理后文件夹仅保留：应用本体（HTML + 双服务端）、三份数据文件、启动/部署/
  停止脚本、图标与文档、tests/、spark-output/、backups/。

## [1.7.0] — 2026-09-05

### 新增：任务移动到其他项目

- **抽屉「所属项目」**：显示任务**当前所在项目**，点击弹出**可搜索的项目切换器**
  （输入即过滤、↑↓ 选择、Enter 确认，对标 Asana 项目切换逻辑）。选中目标项目即移动
  ——子任务、评论、关注人与动态记录一起走，落位到目标项目**同状态分组的末尾**，
  深链与抽屉跟随到新项目页，6 秒内可撤销（撤销同样记录「移回」动态）。
- **批量条「移到项目…」**：多选后一键整批移动，复用同一个可搜索切换器挑选目标项目，
  无写权限的任务自动跳过并在提示中说明；撤销恢复原项目与顺序。
- 权限：需要对**源任务**和**目标项目**都有写权限；跨个人库 ⇄ 共享库由拆库逻辑
  自动把任务搬到对应物理文件。
- 访客/非成员看不到切换按钮（显示纯文本项目名）。

### 新增：键盘排序（拖拽的键盘等价路径）

- 聚焦任务行或侧栏项目后按 `Alt+↑` / `Alt+↓`：任务在**状态分组内**上下移动，
  项目在项目列表内上下移动，焦点跟随，效果与拖拽等效并同样落盘。
- 应用内帮助（?）与 README 快捷键表同步更新。

### 修复：medium/low 遗留收尾

- **原生 `confirm()` 清零**（10 处）：导入覆盖、删除项目、复制项目、批量删除、
  删除任务/子任务、删除评论（主/子）、修改角色、停用账户、重置演示数据——
  全部改为自研确认框（统一样式、Esc/遮罩逐层关闭、焦点管理），部分补充取消回调。
- **复制项目可撤销**：与「复制任务」对齐，6 秒内可一键删除刚生成的副本。
- **表单 label 关联**：31 处 `<label>` 补 `for`（含动态弹窗），读屏可正确播报
  输入框名称；剩余 8 处为颜色色板/成员卡片/状态 pill 等非输入控件，无需关联。

### 测试

- 新增 E2E：M1 任务跨项目移动（下拉切换 → 数据落位 → 动态记录 → 抽屉跟随 → 撤销回原项目）、
  M2 键盘排序（Alt+↓/↑ order 互换并还原）。
- 新增 5 条静态守卫（移动函数与入口、原生 confirm 清零、键盘排序、label 关联、
  复制项目撤销）；全量五层：语法 4 / 单测+守卫 174 / 对等 98×2 / E2E 34 全绿。

## [1.6.0] — 2026-09-05

本轮由第二轮**完整产品走查**驱动（Nielsen 十原则 + 七个可检索维度，91 条 findings /
12 blocker / 8 个改版机会点，报告见 `spark-output/audit/flowtask-round2.md`）。
用户对走查的三个要求：UI 简洁大方、使用便利、**数据和账户合理**——本轮优先修后一条。

### 账户与权限：让「数据跟随账户」真正成立（5 个 blocker）

- **会话签发不再只凭自报 uid**：新增 `GET /api/auth-challenge`（只回 uid/盐/算法），
  登录改为「挑战 → 本地算 PBKDF2 证明 → POST /api/session」，服务端常量时间比对
  passHash 后才签发 7 天会话。此前同机任意页面领个令牌就能冒充任意账户。
- **账户表读取脱敏**：`GET /api/db` 返回的账户表不再含 salt/passHash——
  页面从此拿不到任何人的密码哈希；登录所需盐改由挑战端点按用户名下发。
- **账户表写入按字段级守卫**（Node 与 PowerShell 对等）：无会话只能新增普通成员
  （自助注册）；删户、缩水整表、改角色/用户名需管理员会话，改他人密码需本人或
  管理员会话；请求里缺的哈希字段自动用存量值补齐，脱敏表回写不会洗掉真实哈希。
- **管理员不能再把别人的共享项目「收回为个人」**：收回会把项目搬进操作者的个人库
  文件，原创建人从此任何库里都看不到它——现在收回仅限项目创建人本人，管理员用
  成员管理调整协作范围。
- **成员管理权限单源 `canManageProject`**：此前任何可写成员都能通过「＋成员」
  弹窗增删共享项目成员（与项目设置里「仅创建人或管理员可修改」的自述矛盾）。
- **项目设置的名称/描述同样仅管理权可改**：此前任何成员都能改写共享项目的名称与
  描述，且项目名未转义直接进共享确认弹窗——构成可用的存储型 XSS 攻击链
  （任何成员改名 → 创建人/管理员打开确认框即在其会话里执行脚本）。已转义。

### 数据不丢：把「已保存」变成真的已保存（5 个 blocker）

- **Node 版写盘失败被静默吞掉**：`fs.writeFile/rename` 的错误此前被忽略，
  文件没写成仍回 200「已保存」并推进 rev+hash，之后同内容推送永远命中幂等分支、
  再也不真正落盘。现在失败回 500、版本回滚；PowerShell 版同场景本就回 500，
  并补上 `Move-Item` 目标是目录时静默移入的陷阱。
- **并发写竞态**：rev/hash 的推进从异步写盘回调里提前到排队前同步完成——
  两个并发 POST 不再都读到旧版本、双双 200、后写者静默覆盖。
- **备份轮转误删**：旧版单文件的前缀 `flowtask_data_` 会把所有个人库备份
  （`flowtask_data_<uid>_时间戳.json`）当成自己的备份按时间删掉——改为按
  「完整文件名 + 时间戳」正则匹配，两版对等。
- **登出/切账户不 flush**：防抖窗口内的最后一次编辑会随 `DB/ME` 置空一起作废；
  「导入后当前账户不在新数据里」路径的整库导入结果此前无任何留底地消失。
  现在 logout 先 `flushAllSaves()` 再清状态。
- **账户表空表写回**：一次瞬时读失败 + 一次注册即可用「只剩自己」的表顶掉真实
  账户表并自封管理员——现在读取失败会复位版本基线并拒绝本次会话的一切账户表写回。
- **导入深校验 + 渲染兜底**：`memberIds`/`comments`/`activities`/`subtasks` 缺失的
  备份此前会先落盘再让渲染链 TypeError → 全站白屏且刷新无法恢复。现在导入前
  深校验（说人话的报错）、迁移期结构兜底、`renderContent` try/catch 三道防线。
- **读错误 ≠ 文件不存在**：服务端此前把 EBUSY/EACCES 一律回 204，客户端当成
  「空库」展示并可能触发迁移/演示注入；隔离失败也曾谎报 204。现在只有 ENOENT
  才是 204，其它读错误回 500；前端对 5xx 中止加载并明确提示，绝不进入空应用。
- **409/401 不再被当成「服务挂了」**：推送失败分因处置——401 走会话过期重新登录，
  409 走冲突弹窗，只有网络层失败才降级浏览器缓存模式。
- **服务恢复重连不再静默整片替换远端新库**：掉线期间的本地编辑改走
  「留底 + 冲突弹窗 + 用户决策」，与 409 路径同一套处理。
- **账户表写盘接入顶栏保存状态**：注册/改密/停用此前成功失败都无声。

### 使用便利与反馈

- **多选批量条跨页悬浮**（E2E 探针实证）：挂在 body 上的批量条离开项目页仍悬浮
  且「删除」可点——现在离开项目页自动退出多选并撤条；Esc 也能退出多选
  （按钮早就承诺了「退出多选（Esc）」）。
- **批量操作后计数造假**（探针实证）：批量条显示「N 项已选」而实际选中已是 0——
  现在先清选中再执行；刚进多选也不再挂「0 项已选」的空条。
- **快速添加连续录入**：创建后清空标题、保留项目/负责人/日期上下文并保持弹窗，
  批量录任务不必反复呼出；Esc/取消随时退出。Enter 现在只点**栈顶弹窗**的确定按钮
  （此前 `$('.modal-ok')` 全文档第一个匹配，弹窗堆叠时会误触发下层确认框）。
- **只读视角的假可点控件**：抽屉完成圆点此前是 pointer 外观但点了毫无反馈
  （探针实证），现在给出明确的拒绝说明；列表/看板拖拽被拒的归因也会区分
  「访客角色」与「非项目成员」（此前一律说「你是只读访客」）。
- **批量指派限定项目成员**：此前列全员，指派给非成员后对方「收到通知 → 点开被拒
  → 我的任务里又看得见」；快速添加的 @人名解析上下文同步收窄。
- **「我的任务」与首页待办只列打开得了的任务**，与抽屉的可见性判据对齐。
- 指派/改期弹层无 loading 的重操作（复制项目）、原生 confirm 与自研确认框并存等
  反馈一致性问题：清空回收站已改用自研确认框（后果说明 + 备份提示），其余列入遗留。

### 界面降噪与可达性

- **首页两个一模一样的红色主按钮**（探针实证）：统计行的「＋ 添加任务」降为描边
  按钮，顶栏保留唯一主操作。
- **看板卡不再重复状态 pill**：列头就是状态，卡内再放一个是同屏说两遍。
- **收件箱降噪**：每行的品牌红方块 + 🔔 换成动态发起人的彩色首字头像，
  「未读」的强调不再被整列的红色冲平。
- **登录卡的演示账户块折叠**为 `<details>`，首屏回到「输入 → 登录」。
- **键盘可达**：退出登录/切换账户的用户菜单是原生按钮了（此前键盘用户**永久无法
  登出**，探针实证）；抽屉的键盘增强改到重绘之后执行（此前新抽屉里所有控件
  都没有 tabindex/role，探针实证）；`add-task-row`/看板加卡/日历溢出/筛选摘要/
  popover 条目等一批已绑定的 div/span 补进键盘增强白名单。
- **读屏**：toast 容器与顶栏保存状态补 `role="status" aria-live="polite"`
  （此前全文 0 处 aria-live）。
- **对比度**：辅助小字 `--text-faint` 2.6:1 → ≈4.6:1，已完成/待办状态字色加深，
  主按钮改用深一档品牌红（白字 ≈3.9:1 → ≈5:1）。
- **批量条去 emoji**：六个按钮的 👤📅🗑✓↩✕ 混排改为纯文字（上轮 audit-43 残留）。

### 术语与文案（对照 README 术语表）

- 看板空态「卡片」→「任务」；项目概览把创建人标成「负责人」→「项目创建人」；
  「卡片已放回原位」→「任务已放回原位」；共享确认不再抛出
  `flowtask_shared.json` 物理文件名；「清空回收站」用「彻底删除」；
  偏好项「新建任务的默认状态」补上「已完成」选项。
- 登录页「首次打开自动创建」的演示账户在 file:// 兜底模式下此前实际永远创建不出
  （没账户就登不进去、登不进去就不会创建）——本地模式在进入登录页前补种。

### 测试

- 五层全绿：语法 4 / 单测+集成+守卫 169 / PS1 冒烟（并入 flowtask_test）/
  双服务端对等 98（Node 49 + PS 49，新增脱敏、登录挑战、证明、守卫、写盘失败回 500、
  哈希补齐等断言）/ 真浏览器 E2E 32 条（账户隔离、共享、免登录、假冲突、
  迁移等关键场景全部通过，登录流程已适配挑战-证明协议）。
- 新增 26 条 v1.6 静态守卫（禁词、权限单源、转义、aria-live、批量条行为等），
  防止走查问题回潮。
- 修复走查轮自己引入的两处问题：挑战端点误拒「同源 GET 不带 Origin」导致托管页面
  无法登录（两版对等修复，对等测试新增同源挑战断言）；账户表写守卫此前把
  「脱敏表缺字段」当成字段变更误拒。

### 已知遗留

- 共享库仍是单文件全量写（大团队应改按项目分文件 + 增量写）；标签/筛选视图存在
  个人库里，共享项目的标签在别人视角可能显示不出来。
- 原生 `confirm()` 仍用于导入覆盖、删除项目、批量删除、停用账户等处（与自研
  确认框并存）；复制项目无 loading 与撤销。
- 表单 `<label>` 尚未逐个关联 `for`；三处拖拽仍无键盘/触屏等价路径；完整
  WCAG 2.1 AA 审计未做。甘特在窄屏仍是横向卷轴。
- 本机威胁模型下的残余面：登录前页面仍可自助注册普通成员（产品语义如此）、
  旧版单文件兼容期内仍可凭令牌读取 `flowtask_data.json`（迁移完成后可删）。
- z-index 16 档、圆角 11 种、字阶档位外取值等令牌纪律问题只解决了可访问性相关
  的最小集，全面收敛需先决出 DESIGN.md 与实现的基准关系。


## [1.5.3] — 2026-09-04

### 修复：首页「今天要处理」空态里的「＋ 添加一个任务」点了没反应

绑定写成了短路取首个：

```js
const hqa = $('#home-qa') || $('[data-home-qa]', c);   // 永远命中前一个
if(hqa) hqa.onclick = ()=>openQuickAddModal();
```

统计行里的 `#home-qa` 只要用户有建项目权限就**总是存在**，所以 `||` 永远在它这里短路，
空态里的 `[data-home-qa]` 从头到尾没被赋过 `onclick` —— 点它自然毫无反应。
现改为把两个入口都绑上（`$$` 遍历），"新建第一个项目"同样按多个处理。

### 测试

- E2E 新增 Z2：先造出"今天要处理"空态，断言此时统计行按钮与空态按钮**同时存在**，
  再分别点击，两者都必须打开快速添加；用例结束时还原被临时清掉的日期
- 有效性验证：把绑定改回旧的短路写法后 Z2 确实失败，改回修复版后通过
  （回归测试必须能在有 bug 的代码上红，否则等于没写）
- 全量：语法 4 / 单测+集成 141 / PS1 冒烟 15 / 双服务端对等 66 / E2E 32 全绿

## [1.5.2] — 2026-09-04

### 修复：双击启动时闪过的黑色命令行窗口

`启动 FlowTask.vbs` 原先用 `sh.Exec("cmd /c where powershell")` 和 `where node` 探测运行时。
`cmd` 是控制台程序 —— 即使只是查个路径，也会分配一个控制台窗口闪一下。
现改为**纯文件系统探测**：PowerShell 直接查 `%WINDIR%\System32\WindowsPowerShell\v1.0`
（SysWOW64 兜底），Node 遍历 `PATH` 里的目录找 `node.exe`，再回退到本机运行时版本目录取最新版。
全程不起任何子进程。冷启动实测：双击后 1 秒内服务就绪，无窗口闪现。
单测加了防回退守卫：启动脚本里出现 `.Exec(` 即判失败。

### 新增：个人设置 → 偏好设置

| 偏好 | 选项 | 作用 |
|------|------|------|
| 进入项目时默认打开 | 概览 / 列表 / 看板 / 日历 / 进度 | 点侧栏项目、首页项目卡、搜索结果里的项目时直接进该页面 |
| 打开应用时进入 | 上次浏览的位置 / 首页 / 我的任务 | 冷启动落点 |
| 任务行显示几个标签 | 不显示 / 1 / 2 / 3 / 全部 | 超出的收纳成 +N，悬停可看全部 |
| 新建任务的默认状态 | 待办 / 进行中 | 快速添加未指定状态时的默认值 |

- 偏好存在本机 `flowtask_ui`（localStorage），**不写任务数据文件**：
  改偏好不会产生新版本号，也不会与同事的共享库冲突（E2E 有断言）。
- 非法值（旧数据、手改 localStorage）一律回退默认，不会把界面搞坏。

### 顺带修掉：偏好改动暴露的一处真实缺陷

侧栏项目拖拽排序原先从 `data-nav` 反解项目 id；默认页面偏好给 hash 加上 `/overview` 后缀后，
这个反解就错了（表现为项目拖不动）。改为显式 `data-pid`，不再依赖路由形状，
并加了"不许再从 data-nav 反解 pid"的检查。

### 版本字符串对齐

前端 `APP_VERSION`、Node 服务端、PowerShell 服务端统一到 1.5.2（此前服务端停在 1.5.0）。

### 测试

- 单测 141 条：新增偏好默认值与非法值兜底、`projectHash` / `startHash`，
  以及启动脚本"不得起 cmd 子进程"的守卫
- E2E 31 条：新增 Z1 —— 改默认页面后点侧栏项目真的落到看板；标签设为"不显示"后任务行 0 个标签；
  刷新后偏好仍在且**没有产生新的数据版本号**
- 全量：语法 4 / 单测+集成 141 / PS1 冒烟 15 / 双服务端对等 66 / E2E 31 全绿

## [1.5.1] — 2026-09-04

修掉五个使用中的问题，过程中又挖出两个更严重的隐藏缺陷（数据误判级联、账户表可被覆盖）。

### 你报的五个问题

1. **创建项目点「放弃并关闭」关不掉** — 确认框自己也在弹窗栈顶，回调里再调
   `closeTopModal(true)` 关掉的其实是确认框，原弹窗留在原地。改为显式关闭
   "发起确认的那个弹窗"（`forceCloseModal(mask)`），并连带收掉压在它上面的确认框。
2. **项目不能上下拖动排序** — 新增侧栏拖拽：落点显示品牌色指示线，松手即换位，
   带 6 秒撤销。**顺序只写本机 UI 偏好（localStorage），不写数据文件**——
   否则拖一下共享项目就要重写团队共享库，白白制造冲突机会。
3. **任务不能上下拖动排序** — 实测排序逻辑本身是好的（已补 X1 回归锁住：
   同组换位 + 刷新后顺序仍在）。真正卡住的是下面第 5 条引发的级联状态错乱。
4. **不能添加用户** — 两处：`r.role = role; saveDB()` 里角色是**账户表**字段，
   `saveDB` 根本不写账户表，选的管理员落地后变成员；改为 `saveAuth()`。
   另外入口只对管理员可见，非管理员看不到「成员管理」——README 已写明用 admin 登录。
   新增 Y1 端到端回归：界面建号 → 角色写入磁盘 → 刷新后能用它登录且角色正确。
5. **快捷方式 logo 不对** — 根因在 `make_icon.py`：ICO 的 AND mask 约定是
   **1=透明、0=不透明**，它给不透明像素置了 1，等于把 logo 抠成透明、四角留实心，
   桌面看就是个方块；而且 24bpp 没有 Alpha。
   新增 Node 版 `make_icon.js`（16/24/32/48/64/128 用 32bpp 真 Alpha，256 内嵌 PNG），
   重新生成 `flowtask.ico` 与 `flowtask.png`，并刷新图标缓存；
   同时按约定纠正了 Python 版的 mask 逻辑，避免重跑又生成错图标。

### 顺带挖出并修掉的两个隐藏缺陷

- **一次登出会让整个应用"以为服务挂了"**：`saveDB` 的推送有 400ms 防抖。
  若在这 400ms 内登出，定时器触发时 `ME` 已空，推送返回 false 被当成"存储服务不可用"，
  `SVC_MODE` 置 false → 之后登录只能拿本地兜底会话 → 所有数据读返回 401 → 级联失败。
  修：没东西可推不算失败；登出后作废这次推送；401 按"会话过期"处理（清会话回登录页），
  不再误判为服务宕机。
- **加载过程中不许推送**：`migrateData()` 在 `loadMyStores` 中途会 `saveDB()`，
  那一刻版本号、会话、数据都还是上一个账户的，推送必然出错并触发上面的误判。
  现改为加载期间只记脏标记，合并完成后统一落盘一次。
- **账户表可被旧快照整体覆盖**：任何持旧内存快照的页面写账户表，都可能把别人刚建的
  账户抹掉。现改为写之前先读回最新账户表并**按 id 合并**，陈旧写入不可能再丢账户。
- **账户表读取失败被当成"没有账户表"**：一次瞬时错误就会触发"建演示账户"并覆盖真实账户。
  现在严格区分"文件确实不存在(204)"与"读取失败"，只有前者才允许初始化。

### 测试

- E2E 30 条（新增 X1 任务拖拽排序、X2 项目拖拽排序、X3 放弃并关闭、Y1 界面添加用户）
- 语法 4 / 单测+集成 130 / PS1 冒烟 15 / 双服务端对等 66 / E2E 30 —— 全绿

## [1.5.0] — 2026-09-04

本轮解决三件事：**数据跟随账户**、**不再每次进入都弹数据冲突**、**双击带 logo 的快捷方式即可用**。
所有数据文件仍是 .json（公司电脑会锁定被编辑过的 Office/txt/pdf，json/md/html/db 不受影响）。

### 新增：数据按账户分开（三份物理文件）

| 文件 | 内容 | 谁能动 |
|------|------|--------|
| `flowtask_auth.json` | 账户表：用户名、显示名、角色、PBKDF2 口令哈希 | 注册/登录/成员管理 |
| `flowtask_data_<账户id>.json` | 该账户的个人库：项目、任务、通知、回收站、标签、筛选视图 | 只有本人 |
| `flowtask_shared.json` | 团队共享库：被设为「共享」的项目及其任务与通知 | 项目成员 |

- 内存里仍是单一合并视图（个人库 ⊕ 共享库），保存时按项目的 `scope` 自动拆回两个文件；
  项目连同其任务/通知/回收站条目一起迁移，切换归属是一次原子写盘。
- 服务端按文件名白名单校验（`flowtask_auth.json` / `flowtask_shared.json` /
  `flowtask_data_<id>.json`），杜绝路径穿越；**会话里的 uid 决定能读写哪个个人库文件**，
  前端无法指定别人的文件。
- 账户表与数据库分开记账版本号：改密码、改角色不再无谓地顶高任务数据的版本号。

### 新增：登录门禁与 7 天免登录

- 登录/注册成功后由服务端签发 **HMAC-SHA256 会话令牌**（密钥持久化在
  `flowtask_secret.json`，服务重启后旧会话仍然有效），有效期 7 天。
- 未登录时只能读写账户表：个人库与共享库一律 401，内存里不存在任何任务数据。
- 关闭浏览器再打开仍免登录；过期或密钥变更则回到登录页。顶栏用户菜单可主动「退出登录」。

### 修复：每次进入都弹"数据冲突"

根因是两处叠加，**之前那不是真冲突**：

1. 服务端把「来版 ≤ 已存版」一律判为 409，而前端启动时会原样回写刚拿到的版本号 →
   必然撞自己的版本。现在服务端先比内容哈希：**同版本 + 内容一致 → 幂等 200**，
   内容确实不同才返回 409。
2. 前端启动时无条件推送。现在推送前比对"上次写入内容"，
   **只有真正变化过的那半个库才会被写盘**；不同账户各写各的文件，天然不冲突。

真冲突发生时仍然弹窗，但会说清是**哪个库**（个人库 / 团队共享库）、双方版本号、
差异处数与留底文件名，并保留三个动作：先看最新数据 / 下载我的副本 / 保留我的改动。

### 新增：项目共享与收回

- 项目设置里「🌐 共享给团队 / 🔒 收回为个人」，切换前说明影响范围
  （多少人会看到、多少个任务跟着走、其他人会不会看不见），带 6 秒撤销。
- 侧栏共享项目带 🌐 标识；数据管理页列出三个文件各自的用途、条目数与版本号。
- 演示数据自带一个共享项目 + 一个个人项目，换账户登录即可看到隔离效果。

### 变更：启动方式

- 双击带 FlowTask logo 的快捷方式即可：自动探活 → 未运行则静默启动服务（PowerShell 优先、
  Node 兜底）→ 打开浏览器；同时在应用目录与桌面各刷新一个带 logo 的快捷方式。
- 探活改用 `/api/ping`（免鉴权）。旧版探 `/api/db`，加登录门禁后会返回 401 被误判为"服务没起"。
- Node 兜底路径不再写死版本号，改为扫描运行时目录取最新版（原写死的 `22.22.2` 已失效）。
- 启动脚本源码改为纯 ASCII，避免不同系统代码页把脚本本身弄坏。

### 兼容与升级

- 旧版单文件 `flowtask_data.json` 会被自动识别：账户拆进账户表，
  首次登录该账户时把内容迁入其个人库，**原文件保持不变**（可自行删除）。
- 新增 `flowtask_secret.json`（服务端会话密钥）与按账户的数据文件，均已加入 `.gitignore`；
  `flowtask_secret.json` 已从版本库中移除跟踪。

### 测试（五层 239 条断言全绿）

- `tests/syntax_check.js` 4/4：内联脚本编译期校验 + **顶层重复声明检测**
  （本轮新增，当场抓出一处同名函数静默覆盖真实实现的事故）
- `tests/flowtask_test.js` 130/130：新增 `splitStores`/`mergeStores` round-trip、
  通知与回收站按项目分流、三文件版本号独立、v1.5 契约守卫
- `tests/flowtask_test.js ps1` 15/15
- `tests/server_parity.js` 66/66（新增）：同一套 33 个场景分别跑 Node 与 PowerShell 实现，
  断言行为完全对等（含 401/403/幂等 200/真冲突 409/路径穿越/篡改会话）
- `tests/flowtask_e2e.js` 24/24：真浏览器（Edge headless + CDP）新增
  V1 未登录拿不到数据、V2 两账户互不可见、V3 共享与收回跨库移动、V4 7 天免登录、
  **V5 反复进入不弹冲突**（本轮核心诉求的回归锁）
- 测试全程用 `FLOWTASK_DATA_DIR` 隔离到临时目录，并校验真实数据文件未被改动

### 顺带修掉的实现缺陷

- `saveDB` 不再自增 `meta.rev` 后，渲染索引失去失效信号 → 新建的任务不进列表；
  改为显式数据纪元 `_dataEpoch` 驱动索引重建
- 「下周X」日期算法在当天已晚于目标星期时会多算一周
- 窄屏布局视口被顶栏与收起态抽屉撑宽（390px 实测撑到 560px）
- PowerShell 版在 8.3 短路径（`SID~1.SHE`）下 `Move-Item` 报「对象不存在」→ 启动时规范化长路径
- 集成测试偶发失败源于上一轮残留服务进程 → 新增端口占用清理

### 已知遗留

- 共享库是单文件全量写：团队规模大（共享任务上千条）时应改为按项目分文件 + 增量写
- 标签与筛选视图存在个人库里，共享项目的标签在别人视角下可能显示不出来
- 个人项目收回时，别人在该项目中产生的评论随项目一起进入你的个人库（不会丢失，但别人看不到了）
- 完整 WCAG 2.1 AA 审计仍未做

### 本轮收尾时顺带修掉的两个真实缺陷

- **升级会"丢数据"**：账户表已存在时旧库不会被读取，且迁移判据用了"一个项目都看不到"
  （共享库只要有项目就永远不成立）。现改为个人库文件缺失即惰性读取旧库，并
  **按项目创建人分配**——谁的项目进谁的个人库，先登录的人不会整体认领别人的数据；
  回归见 E2E 的 W1 / W2
- **隔离响应竞态**：损坏文件隔离在 `fs.rename` 未完成时就回了 204，客户端立刻检查
  "是否已改名"会偶发扑空（约每 2-3 轮复现一次）。现改为 rename 完成后再回应，
  失败时不带隔离头以免谎报；单测连跑 4 轮 130/130 稳定

## [1.4.0] — 2026-09-03

本轮由一次完整的**产品设计走查**驱动（Nielsen 十原则 + 响应式 + 性能感知共 12 个维度，
54 条 findings / 8 个改版机会点，报告见 `spark-output/audit/flowtask.md`），
按 A→I 九个阶段分提交实施，每阶段跑测试后再提交，可逐段回滚。

### 修复（正确性与"数据无声消失"类 blocker）

- **`4 blocker` 之一**：分组头「＋」永远预选「待办」——v1.3 把属性改名为 `data-st-add` 后，
  绑定处仍读 `dataset.secAdd` 取到 `undefined`（提交 `19cf54e`）
- **`4 blocker` 之二**：抽屉删除按钮写「永久删除任务（不可恢复）」而实际是移入回收站且可撤销，
  文案与行为相反；改为「删除任务」+ 一行后果说明
- **`4 blocker` 之三**：409 版本冲突时本地改动被静默覆盖、副本在界面里找不回来。
  现在会留底 → 采用远端 → 登记副本 → 弹出可决策对话框
  （先看最新数据 / 下载我的副本 / 保留我的改动），并在「数据管理 → 冲突副本」列出历史留底
- **`4 blocker` 之四**：弹窗里未提交的输入按 Esc 或点遮罩即丢；且 Esc 一次关掉所有层叠弹窗
  （与 README 承诺的"逐层关闭"不符）。重建模态框基础层：`MODAL_STACK` 逐层关闭 +
  未保存守卫 + 焦点陷阱与焦点归还 + 主按钮 in-flight 锁 + `role=dialog`/`aria-modal`
- 起止日期倒挂：开始日期晚于截止日期此前可直接保存并让甘特画出反向条，
  现在自动顺延另一端并提示（`applyDatePair`）
- 导入失败把英文 `SyntaxError` 原文抛给用户；导入校验文案泄漏 `users 应为数组`「id」等术语，
  全部改为用户语言；导入前自动留底当前数据
- 只读访客拖卡片时静默弹回、无任何反馈 → 给出「卡片已放回原位」提示并重绘
- 「下周X」日期算法在当天已晚于目标星期时会多算一周（E2E/单测暴露后修正）
- `renderHome` 内一处 `$` 误写导致 `forEach` 抛错（E2E 暴露后修正）
- 窄屏布局视口被顶栏与收起态抽屉撑宽（390px 实测被撑到 560px）→ 顶栏可收缩规则 + `overflow-x:clip`

### 新增（能力与体验层）

- **顶栏存储胶囊升级四态**：`⏳ 保存中…` / `💾 已保存 HH:MM:SS` / `💾 文件存储` / `⚠ 仅浏览器缓存`，
  侧栏脚注补最近保存时间；本地写盘改为 250ms 合并 + 关页前强制落盘
- **快速添加支持一句话语法**：`@负责人`、`!高/!中/!低`、`#标签`、
  `今天/明天/后天/大后天/周五/下周二/N天后/9月5日/2026-09-05`，
  识别结果实时回显并回填到下方控件；添加子任务同样支持 `@人` 与日期词
- **筛选按「项目 × 视图」各自记住**（存独立 `flowtask_ui`，不再把数据版本号顶高），
  切换视图不再清空筛选；新增**保存筛选视图**（`DB.savedFilters`，团队共享、随备份走），
  可一键套用、可删除且 6 秒可撤销
- **应用内帮助层**：顶栏「?」入口 + `?` 快捷键，含快捷键 / 数据与备份 / 权限 / 分享链接四段；
  首次进入给一次性欢迎引导（只打扰一次）
- **任务级复制**：抽屉「⧉ 复制任务」生成同项目同状态副本，剥离评论与活动记录，可撤销
- **记住上次浏览位置**：打开应用不再每次都回首页
- **三态补齐**：列表分组与看板空列区分"真没有"与"被筛选隐藏（共 N 项 + 一键清除）"；
  首页空态补主动作按钮与"为什么是空"的说明；搜索结果不再静默截断，无匹配可一键清除
- **启动骨架**：探测存储服务期间（最长 1.5s）不再闪登录页、不再纯白屏
- **键盘与触控可达**：渲染后统一 `enhanceA11y` 给 div/span 型可点元素补
  `tabindex=0` / `role=button` / `aria-label`，支持 Enter/Space 激活；
  补 `:focus-visible` 焦点环与 `:active` 按压态；小控件用伪元素把热区撑到 ≥28px
- **窄屏适配**：`≤900px` 侧栏改抽屉式（汉堡 + 遮罩 + 点导航自动收起）、抽屉宽度自适应；
  `≤640px` 首页与概览栅格降单列、顶栏元素按需收缩
- 抽屉头部「复制链接」：深链终于有了分享入口（`copyText` 带 `execCommand` 兜底）
- 批量完成 / 取消完成 / 指派 / 改日期统一有 toast 反馈与 6 秒撤销，并写入活动记录
- 批量条显示"含 N 项被当前筛选隐藏，操作仍会生效"；切换筛选只剔除被隐藏的选择项
- 同名标签直接复用已有标签；新建项目拦截同名
- 弹窗必填校验从满屏飞的 toast 改为字段级行内错误（红边 + 提示 + 自动聚焦 + `aria-invalid`）
- 登录 / 注册按钮在 PBKDF2 计算期间禁用并显示进行中

### 重构（一致性与设计语言）

- **任务状态唯一真相源 `STATUS_DEF`**：名称 / 样式类 / 色板令牌 / 选择器 / 顺序全部派生；
  `:root` 新增 `--st-*`、`--prio-*`、字阶与圆角令牌，
  修掉"同一个进行中在行里是蓝、在选择器里是橙"的三套配色分叉
- **优先级徽章改为「左侧色条 + 彩色文字」**，与实心填充的标签 pill 明确区隔
  （红色标签「紧急」不再伪装成高优先级）
- **术语统一**：优先级全站 高/中/低；看板「＋ 添加卡片」→「＋ 添加任务」；
  项目负责人 → 项目创建人；协作者 → 关注人（13 处，与字段 `followers` 语义对齐）；
  相对时间 `'X小时前'` → `'X 小时前'`；登录页不再谎报"数据完全存储于本机浏览器"
- **弹窗主按钮动词随对象走**：创建项目 / 添加任务 / 添加子任务 / 创建账户 / 发布 /
  保存设置 / 保存成员 / 创建标签 / 设置日期（不再一律退回"确定"）
- 列表行降噪：标签最多 2 个 + `+N` 收纳，有头像时不再重复显示关注人计数
- 首页重排为**单一主焦点**：「今天要处理」（逾期/今天分组置顶全宽）→ 未来 7 天 + 最近讨论 →
  项目概览（带进度条）；4 张大数字统计卡降为一行内联统计
- 删除从未使用的 `.prio-chip` / 通用 `.prio-*` 实心背景等死样式；图标按钮补 `title`

### 测试与质量闸门

- `tests/syntax_check.js`（新增）：把 HTML 内联脚本全部抽出来做编译期校验，
  出错时回显源码上下文——单文件应用没有构建步骤，这是最便宜的一道闸门
- `tests/flowtask_test.js`：53 → **113 条**，新增本轮纯函数覆盖
  （`fmtClock` / `summarizeDBDiff` / `applyDatePair` / `findDupName` / `resolveDateToken` /
  `parseQuickText` / 渲染索引 / 状态唯一真相源）与 **19 条防回退守卫**
  （锁住已修掉的语言分叉、色板分叉、可达性与响应式基础设施、版本号一致性）
- `tests/flowtask_e2e.js` + `tests/flowtask_e2e_ux.js`（新增）：**真浏览器 E2E**——
  Edge headless + CDP（零依赖，用 Node 内置 WebSocket），**18 条全绿**。
  覆盖：＋按钮预选、删除文案、未保存守卫与逐层 Esc、保存态时间戳、一句话解析端到端、
  纯键盘闭环、390px 窄屏、状态色板计算样式、双页 409 冲突找回数据、
  筛选刷新后仍在 + 存为视图、300 条任务单次勾选 < 1200ms、任务复制与撤销、列表行降噪
- E2E 全程用 `FLOWTASK_DATA_DIR` 把数据/备份/冲突副本重定向到临时目录，
  并在结束时校验真实 `flowtask_data.json` 的 size/mtime 未变

### 仓库维护

- 原 `.git` 受损（`refs/` 缺失、`objects/` 仅存 10 个松散对象且无 pack、index 指向失效对象），
  历史不可恢复（无 remote）。已把损坏的 `index` 与 `logs` 移到 `.git/corrupt-backup-*` 留档，
  以 v1.3.0 工作区作为新基线根提交，本轮改动分 9 个提交可逐段回滚

### 已知遗留（下一轮候选）

- 列表/看板仍无虚拟化：300 条内流畅，单项目上千条时建议分组折叠 + 懒渲染
- 172 处 `font-size` 声明尚未全部收敛到字阶令牌；`section-block/sec-*` 等旧命名残留待 sweep
- 浏览器"后退"仍会跳过抽屉（抽屉打开用 `replaceState`）
- 暗色主题、通知桌面提醒、附件、描述富文本与 @提及、截止时间/时段仍未做
- 完整 WCAG 2.1 AA 审计（本轮只补了键盘与语义，未做对比度/读屏全量）

### 路线图更新

| 优先级 | 事项 | 状态 |
|--------|------|------|
| ★★★ | 筛选多选 + 保存筛选（`DB.savedFilters`） | ✅ 本轮完成 |
| ★★★ | 顶栏保存态与冲突可见性 | ✅ 本轮完成 |
| ★★☆ | 快速添加自然语言解析 | ✅ 本轮完成 |
| ★★☆ | 任务复制 / 关注人语义与入口 | ✅ 本轮完成 |
| ★★☆ | 键盘可达 + 窄屏两断点 | ✅ 本轮完成（读屏全量待做） |
| ★★☆ | 人员负载视图（负责人×周热力表） | 待做 |
| ★☆☆ | 任务依赖 + 甘特拖条改期 | 待做 |
| ★☆☆ | 收件箱桌面通知（Notification API） | 待做 |
| ★☆☆ | 标签管理页（CRUD/换色/删引用） | 待做 |
| ★☆☆ | 附件（服务端 `/api/file`，勿 base64 塞 JSON） | 待做 |
| ☆ | 列表虚拟化 / 区域失效重绘 / 暗色主题 / 完整 WCAG | 待做 |



## [1.3.0] — 2026-08-31

### 变更：移除自定义"分区"，分组统一由任务状态驱动

需求收敛：项目不再允许自定义分区列，任务按 **待办 / 进行中 / 已完成** 三种状态
自动分组——该状态即列表与看板的列，与 Asana 默认工作流对齐。

- **新建项目**弹窗移除"初始分区"点选区，改为提示文字说明状态即列，
  新项目不再写入 `sections` 字段。
- **复制项目**不再复制/映射分区；任务状态随深拷贝原样保留
  （此前对已迁移数据执行复制会因 `sections` 缺失而报错，顺带修复）。
- **数据迁移**：`migrateData` 自动删除历史项目的 `sections` 与任务的 `sectionId`
  字段（v1.2.1 及更早数据首次加载后即完成清理）。
- 清理相关命名残留：注释、`data-sec-add` 按钮属性（→`data-st-add`）、
  无引用的 `.sec-drag` CSS；状态/优先级选择按钮保留共用样式。

## [1.2.1] — 2026-08-31

- fix：子任务详情的**描述/标题框宽度**——原样式只写给主任务的 `#dt-desc`/`#dt-title`，
  子任务的 `#ds-desc`/`#ds-title` 无样式退回浏览器默认窄宽度；现选择器合并共用，
  描述框与其他内容等宽对齐。
- feat：**评论删除**（主任务与子任务同时支持）——作者本人可删自己的评论，
  管理员可删任何评论；删除前确认，删除后 6 秒内可"撤销"原位恢复，
  并写入活动记录"删除了评论"。
- 测试复跑 53/53 通过；本环境 Node 运行时升级至 24.14.0（WorkBuddy 内置）。

## [1.2.0] — 2026-08-31

### 变更：子任务升级为完整详情视图（取代 v1.1.0 的"备注"方案）

需求澄清：子任务不是要"备注"，而是要**和主任务一样点开评论**。本次将子任务
能力与主任务完全对齐，唯一区别是不能再嵌套子任务。

- **移除**子任务 `note` 备注字段与 📝 就地编辑 UI；已有备注内容自动迁移为
  子任务"描述"（`desc`），不丢数据。
- **数据模型**：子任务补全 `status / startDate / priority / desc / tags /
  followers / comments / activities / recurring / createdBy / createdAt`，
  `migrateData` 对旧数据自动升级；`done` 与 `status` 保持单一真相源。
- **新增子任务详情视图**（复用右侧抽屉）：
  * 头部"‹ 父任务：xxx"一键返回主任务；支持深链 `#/project/项目/视图/任务/子任务`
  * 标题编辑、状态三选（待办/进行中/已完成）、负责人、开始/截止日期、
    优先级（高/中/低 或 继承主任务）、重复、协作者、标签（含新建）、描述
  * **评论**：发送（Ctrl+Enter）、作者就地编辑、评论通知子任务负责人/
    创建人/父任务负责人/协作者（收件箱新增 sub_comment / subtask_follower 类型）
  * 活动记录：标题/状态/指派/日期/标签等变更留痕
  * 删除子任务：6 秒内可"撤销"
- **交互入口**：父任务抽屉中点击子任务标题或 💬 评论角标进入详情；
  "我的任务"里的子任务行同样直达子任务视图；访客只读可查看全部内容。
- **重复子任务**：设了重复的子任务完成后自动生成下一期（日期顺延、评论清空、
  保留描述/标签/指派），与主任务重复逻辑一致。
- **克隆一致性**：主任务重复生成与项目复制时，子任务评论/活动重置，不带旧数据。
- 全局搜索改为匹配子任务"描述"；测试新增 6 条子任务状态断言（53 条全绿）。

## [1.1.0] — 2026-08-31

本次优化共 6 个提交，配套分析报告见会话交付物《FlowTask_分析与优化计划》。

### 修复（正确性） — `4ed1365`

- **时区 Bug（高影响）**：`todayStr()/dateOffset()/nextDueDate()/甘特日期` 用
  `toISOString()` 取 UTC 日期，东八区每天 0:00–8:00 "今天"被算成昨天，
  首页统计、逾期标红、日历高亮、重复任务推期全部错位。新增 `localDateStr()`
  统一替换 5 处，并有单元测试覆盖。
- 列表视图分区操作按钮悬停选择器笔误（`.sec-head`→`.section-head`），桌面端按钮不再隐身。
- 快速添加快捷键从 `Ctrl+Q`（与浏览器"关闭窗口"冲突）改为真正的 `Tab` 后接 `Q` 序列。
- 取消完成任务时恢复原状态（快照 `_prevStatus`），不再强制回"待办"。
- 归档项目只读化：禁写 + "已归档"横幅 + 管理员一键取消归档。
- 注册用户名文案与正则统一；清理启动死代码与错误注释；
  `migrateData` 强制 `completed` 与 `status` 一致（单一真相源前置）。

### 新增：子任务备注 — `c6a8f9e`

- 子任务增加 `note` 字段（多行文本），旧数据自动迁移，旧备份导入兼容。
- 抽屉内 📝 按钮 + 行下直接展示备注全文；就地编辑（Ctrl+Enter 保存 / Esc 取消）。
- 新建子任务弹窗带备注输入框；访客只读；重复任务克隆与项目复制自动携带。

### 数据安全与一致性 — `9003dbb`

- **服务端鉴权（Node 与 PowerShell 对等实现）**：启动生成随机令牌，写数据必须携带；
  CORS 从 `*` 收敛为仅回显可信来源；请求体上限 20MB→8MB；POST 校验数据结构；
  响应统一 `nosniff + no-store`；页面托管自动注入令牌。
- **版本冲突保护**：`meta.rev` 自增；旧版本写入返回 409；新增 `/api/db-conflict`
  `/api/token` `/api/version` 端点；冲突时本地副本留底并采用服务端新版。
- **自动备份**：写入前间隔≥10 分钟备份到 `backups/`，保留最近 40 份。
- **损坏自愈**：数据文件损坏自动隔离为 `corrupt_<时间>.json`，消除启动白屏边界。
- **回收站**：任务/项目/分区删除全部软删除，30 天自动清理，侧边栏回收入口、
  恢复/清空页、删除带 6 秒"撤销"。
- 保存失败立即回滚存储指示（修复"显示文件存储实际没存上"的失真），30 秒自动重连。
- 多标签页同步（BroadcastChannel + visibilitychange 版本核对）。
- 修复 PS1 请求体短读拼接脏字节。

### 安全加固 — `83d1568`

- 密码哈希升级 PBKDF2-SHA256（10 万轮），旧哈希登录时透明升级。
- 通知结构化存储，渲染统一转义；旧通知白名单净化（仅保留 `<b>/<br>`），
  杜绝导入备份注入脚本；批量指派通知可点进任务。
- 导入校验升级（必备数组/类型/ id 唯一），导入后检查会话账户有效性。
- 页面 CSP（禁外部资源）+ 服务端 `nosniff`。

### 性能与体验 — `712f1ce`

- 全局搜索：200ms 防抖；匹配描述/子任务标题/子任务备注；↑↓ 键选择 + Enter。
- 任务深链：打开抽屉写入 `#/project/项目/视图/任务`，可复制链接、刷新恢复。
- 我的任务含"我负责的子任务"分组；日历展示子任务截止日期（"子"标签）。
- 收件箱"全部标为已读"；活动记录每任务截断保留最近 200 条。

### 测试 — `f2f27e7`

- `tests/flowtask_test.js` 58 条断言全绿：单元 21（日期/转义/校验/净化）+
  Node 服务端集成 26 + PowerShell 冒烟 11。
- 修复 `flowtask_server.ps1` 缺 UTF-8 BOM 导致 Windows PowerShell 5.1 解析失败。

## [1.0.0] — 基线（`3b07873`）

优化前原始版本：单文件前端 + 双存储服务，仿 Asana 核心功能完整，
但存在本次修复的时区、鉴权、数据覆盖等问题。

---

## 路线图（尚未实施，按优先级）

| 优先级 | 事项 | 预估 |
|--------|------|------|
| ★★★ | 筛选多选 + 保存筛选（`DB.savedFilters`） | 1 天 |
| ★★★ | 收件箱桌面通知（Notification API） | 0.5 天 |
| ★★☆ | 标签管理页（CRUD/换色/删引用） | 0.5 天 |
| ★★☆ | 任务依赖 + 甘特拖条改期 | 2~3 天 |
| ★★☆ | 人员负载视图（负责人×周热力表） | 1 天 |
| ★☆☆ | 附件（服务端 `/api/file` 存 `attachments/`，勿 base64 塞 JSON） | 1~2 天 |
| ★☆☆ | 描述轻富文本 + @提及 | 1 天 |
| ★☆☆ | 截止时间/时段（Asana 式 上午/下午） | 1 天 |
| ☆ | 暗色主题 / 响应式侧边栏 / 平板拖拽 / 无障碍补全 | 各 1~2 天 |
| ☆ | 区域失效重绘 + 索引层 + 甘特缩放（数据量大时再做） | 3~5 天 |
